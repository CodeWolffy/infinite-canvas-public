---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

## Go 公益平台

当前分支已切换到 `server/` Go 服务。以下事项对应本次实际变更；下方公司版记录不代表 Go 平台已经通过同样的人工验收。

### 邀请返利、模型配置与本机 Docker 验证

前端与 Go API 镜像均已构建并在 `http://localhost:3301` 启动，TypeScript 检查通过。隔离 Docker 数据库、Redis、MinIO 下，后端 50 组测试（含子用例共 67 项）与前端完整 53 项回归全部通过，无失败或跳过；7 组经 Web Nginx 的接口场景也已通过。

- 邀请奖励已改为好友真实充值到账后按管理员 `referralPercent` 配置返利；注册只建立关系，签到与兑换码不返利。验签、重复回调、比例变化、不补发和失败回滚已由集成测试验证，浏览器邀请页显示当前百分比及累计返利。
- 模型价格显示有效小数位，`0.008` 不补成 `0.008000`；后台模型列表和文本模型菜单已验证。
- 模型状态页与接口只允许登录后访问，顶部导航和个人中心入口已验证；页面保留工作台布局，文本对话侧栏移除专属状态入口。
- 管理员按模型配置 `maxOutputTokens` 和 `reasoningEfforts`。可选档位为 `low/medium/high/xhigh/max/ultra`，按实际渠道能力勾选；未配置思考档位时仅用自动，用户端不展示输出上限。后台保存、前台更新、无效档位拒绝及三种协议传参均已验证。
- 当前对话按 `conversationId` 携带历史消息，已验证续聊、切换模型、刷新恢复和新对话隔离；新建对话清空系统提示词与思考设置，不建立跨对话用户记忆。
- 浏览器深浅主题和核心操作已验证，无脚本错误；移动端细节、真实上游、商户支付、生产 HTTPS 反代仍待验收。

替换服务前已备份 PostgreSQL、MinIO、Redis 和 `.env.local` 并验证备份；启动后余额、账本、签到、加密 Key、模型与运营设置摘要一致，数据库保持版本 13。日志、结果与备份位于本机忽略目录 `data/local-verification/20260912-164419/`。

### 渠道调度与开放能力待验收

此前渠道扩展已完成本机 Docker 构建、启动与回归：后端 44 组测试（共 55 个含子用例的通过记录）及前端 52 项全部通过，0 失败、0 跳过。当前版本完整验证结果以上方记录为准。

已有本机数据库从版本 11 升至 13，升级前备份 PostgreSQL、MinIO、Redis 和 `.env.local`；钱包、账本、签到及加密 Key 的数量与内容摘要前后相同。`http://localhost:3301` 的 API、Web 与三项存储依赖运行正常，健康、登录、文本、状态、素材页及 JS 入口均返回 200。

独立数据库与模拟上游下的 7 组接口验收全部通过，覆盖 Nginx 登录、单次冻结与故障转移、Key 去重/停用、审核通过与拒绝退款、Claude 原生协议及用量、检测停用恢复、匿名状态、邀请/签到奖励和图片存储；公开状态页浏览器渲染正常，无脚本错误。真实上游、商户支付、SMTP、生产 HTTPS 反代、备份恢复演练及持续压测仍需验收。

- 永久余额：分组页面不再出现周期赠送、周期消费上限，周期领取接口应返回 404；升级后原余额、冻结额、签到与历史账本不变，兑换码继续可用。本月统计不改变余额。
- 自动切换：默认每任务最多 3 次（含首次），按优先级和权重选候选；限流、超时和上游故障切换，鉴权失败停用对应 Key。核对不同实例继续同一任务时只冻结一次、成功只结算一次；耗尽或没有剩余候选后全额退回冻结。
- 不重复提交：内容政策、无效参数直接失败；流式已输出文本、已取得视频任务 ID、或上游结果已成功返回后，不再次发起生成。服务重启与迟到响应不得突破尝试次数、覆盖取消结果或重放已知拒绝。
- 多 Key：验证追加去重、轮询/随机、单独启停与删除；一个 Key 鉴权失败不能停用其他 Key 或整条渠道。已受理视频在更换 Key 后仍按原快照续查，管理接口不返回明文。
- 健康处置：连续 3 次有效检测失败停止接单，冷却后按原配置检查，1 次成功恢复；手动停用不自动开启。无效参数、内容拒绝和本地存储错误不累计为健康故障。未开启自动检测时需手动触发恢复检查。
- 耗时与模型状态：任务/探针/调试成功后，渠道列表 p50/p95 与保存样本一致；登录后访问 `/status` 和 `/api/status/models`，未检测及自动检测过期显示待检测，部分渠道可用时显示部分可用；响应不包含密钥、地址或检测提示词。
- Claude Messages：核对原生请求头、系统提示词、图片附件、SSE 完成标记及输入/缓存/输出用量。用户生成采用管理员配置的最大输出 token，达到上限正常结算；管理员检测和调试台仍可填写独立参数。
- 人工审核：`review` 进入待审核，`log` 放行仅记规则，`block` 优先拒绝；文本及图片/视频/音频提示词、文本参数均检测。验证普通用户不能审批，通过后入队，拒绝/取消退款幂等，批量请求和重新手动重试不绕过审核；被拒绝的文本不会成为下一轮上游上下文。
- 邀请返利：默认关闭，推荐链接不能替代注册邀请码；现行规则为上方的真实充值比例返利，余额永久保留。
- 用户文本对话：`/text` 支持管理员配置的模型与思考档位、报价、系统提示词、历史续聊、流式回复和服务端取消；刷新恢复历史与思考设置，最终消息只出现一次；换账号关闭旧订阅，不显示旧会话或写回旧请求结果。
- 任务适配器：现有 OpenAI 兼容视频、Gemini/Veo 经统一适配器提交与续查；核对时长与价格快照不变。即梦、可灵、Vidu、原生 Sora、Suno 只有待接入入口，收到实际 API 文档后才能接入验收。

### 此前审查修复记录

- 启动迁移：已接入渠道软删除、独立上传中状态和检测截止时间迁移（`009`–`011`）；分别检查空库启动和已有 Go 数据库升级，确认渠道与模型接口可用。
- 成本汇总：用户实付应保持正数；已知成本 ¥1、实付 ¥0.6 时，补贴应为 ¥0.4。
- 上传回收：上传取消、超时或文件就绪状态写入失败后，使用仍有效的清理上下文释放配额；对象删除失败时保留待删除记录，由既有清理任务继续回收，正在上传的文件仍预占配额且不得误删。
- 主动检测：配置统一使用 `bindingIds`，已有检测配置需重新选择绑定并保存；同一平台模型的两个上游绑定应分别按所选模型和成本配置执行，其他渠道的绑定应被拒绝。
- 渠道调试：与正式生成共享并发槽位，繁忙或冷却时排队，拿到槽位后沿用渠道执行超时，不再固定截为 60 秒；取消或离开页面后不重发请求。成功、失败及中断调用均应保留独立成本记录，不扣用户余额；成本配置不明确时保持未知。服务重启后，未完成检测记录按原截止时间结束。
- 调试结果：展示真实 HTTP 状态、原生 JSON 或 SSE 事件内容；文本显示首字耗时，图片显示预览。渠道密钥须脱敏，大型原始响应只在展开或复制时格式化。
- 素材写入：新增、编辑、删除及导入均等待服务端成功，失败时保留输入并显示错误；提示词库、视频页保存和画布侧栏删除遵循同样规则。素材包导入保留文本、图片、视频、音频，作为当前账号的私人副本保存，并按实际成功数量提示。
- 画布导出：从素材库插入的图片应完整进入 ZIP，跨账号导入后仍能显示；文件准备、保存或导出期间切换账号时，旧回调不得写回或触发旧文件下载。
- 恢复码通知：恢复码登录关闭两步验证、消费恢复码、撤销会话和重新绑定提醒入库在同一事务内完成；开启邮件时同步写入安全通知投递队列。

本机 Docker 已验证：前后端镜像构建通过，后端 36 组测试（含 39 项用例）和前端 52 项回归全部通过、无跳过；已有本地数据库从版本 8 升到 11，原数据卷与密钥保持不变。升级前已备份 PostgreSQL、MinIO、Redis 和本地配置。

隔离数据库与模拟上游下的 7 组接口场景通过，覆盖登录改密、多绑定检测配置、文本/图片调试、首字与 HTTP 状态、响应脱敏、排队取消、媒体上传下载、素材增删及成本记账。浏览器已确认文本结果、HTTP 201 图片预览及脱敏内容；模拟 API 故障后素材输入保留，恢复保存并刷新后仍可见。

本次验证修正了素材导入按钮误用 `loading` 属性的构建错误，并补齐测试辅助逻辑、显式配置并发测试夹具、对齐无响应体接口的 204 断言。另修复 Nginx 将 `/assets` 当成资源目录、重定向到内部端口 3000 的问题，素材页直访与刷新已通过；JS/CSS 资源仍正常提供。真实上游、商户支付、SMTP、生产 HTTPS 反代、备份恢复与持续压测仍待验收。

### 本轮后端能力完善

- 管理后台新增页面展示与布局深度优化：
  - 充值兑换码页面：重构为全宽卡片式布局，顶部增加 4 项核心数据指标卡（总数、可兑换数、已用完/已停用数、本页累计面额），表格列宽精确按比例重构（兑换码标识 220px 带一键复制前缀、单张面额右对齐、使用进度带微进度条、状态居中区分色彩、有效期及创建时间、备注说明自适应），批量生成结果弹窗加宽至 720px 并采用双列网格卡片排版。
  - 用量统计看板：指标卡重整为 12 项无孤单项的双行规整网格布局；下方「按渠道尝试」表格列宽合理分配（渠道名称自适应留白、尝试数与成功数等宽、成功率带色阶微进度条、耗时列等宽对齐），补齐渠道指标 CSV 导出功能。
  - 渠道在线调试台 (Playground)：优化为全屏视口高度适配，在桌面端消除外层滚动条，左侧配置面板与右侧响应面板高度完全对齐撑满并各自内部平滑滚动，空状态与错误/结果居中展示。
  - 通知中心：Popover 浮层宽度拓宽至 `380px ~ 440px`，列表最大高度升至 `380px`，通知条目支持圆角悬浮态与微内边距，提升多行正文阅读体验。
- 通知系统轻量化：消除了 NotificationCenter 2.5 秒短轮询，抽屉改为轻量 Popover 气泡弹层，双 Tab 融合系统消息与全站公告，新增一键全部已读接口 `POST /user/notifications/read-all`。
- 多维用量与成本看板：`GET /api/admin/stats` 扩充 `byDates` 与 `byCapabilities`；用量统计页面新增每日请求与消费走势图，以及能力类别（图像/文本/视频/音频）的调用与消费占比条。
- 充值兑换码批量生成与导出：`POST /api/admin/redeem-codes` 增加 `count` 字段支持一次性批量生成（最多 100 张）；前端提供批量数量选择、生成结果弹窗、一键复制全部及 TXT/CSV 导出文件。
- 在线渠道调试台 (Playground)：新增 `POST /api/admin/playground/test` 管理端调试接口；管理后台新增 `/admin/playground` 页面与侧边栏导航，支持选择任意渠道和模型发送测试 Prompt，实时观测延迟、首字耗时、HTTP 状态码及原生 JSON。
- 上游错误透传与安全政策拦截提示：上游返回包含详细错误（如 `status_code=400, 您的请求无法用于生成图像。该请求可能因安全政策被拦截，或不适合进行图像生成。`）时，系统不再用硬编码的静态文案覆盖，而是完整提取错误原因并识别安全审核政策；写入任务记录并在画布与工作台直接向用户呈现。
- 渠道新建默认参数调整：管理端新建渠道弹窗中，超时秒数默认设置为 300 秒（300000ms），并发默认设置为 20；数据库默认值同步对齐。
- 渠道软删除与模型解绑（待人工验证）：删除已有生成记录或请求日志的渠道时，由硬删除升级为软删除标记（`deleted_at`）并级联解除模型绑定，彻底解决外键冲突导致的无法删除问题；调度与巡检全面忽略软删除渠道。
- 画布与工作台模型价格展示（待人工验证）：画布配置节点选择器支持每项展示价格徽标及选中态价格，修复状态库匹配与单价丢失问题；图片工作台模型下拉选项后附带单张价格 `(¥0.008/张)`。
- 同渠道多上游模型解耦与批量绑定：同一渠道可同时绑定多个不同上游模型至同一平台模型，生成独立 binding ID 不再相互覆盖；渠道管理抽屉支持多选批量绑定；模型管理抽屉与上游成本按 binding 独立呈现、独立配置优先级、权重、启停与成本；调度器核验精确对齐 binding 启停。
- GPT Image 2.5 与质量模式：图像设置面板支持极高（xhigh）与极致（max）模式；`gpt-image-2.5-flare` 与 `gpt-image-2.5-sunburst` 上游请求透传 quality 且自动忽略 response_format。
- 计费回归：超额补扣只结算一次、零价缓存保持免费、计算费用与实际扣款分开记录；按秒价格可保存，字符串/数字时长一致，重试应用当前分组价格；同一用户可兑换不同充值码。验收时同时核对余额、冻结额、任务和日志。
- 渠道运营：配置主动生成检测、模型变更及余额告警；自动检测默认关闭，沿用渠道槽位、超时和冷却，检测中断后记录失败且不重放原生成。真实渠道与 SMTP 下继续验证状态变化通知及发送失败记录。
- 账号安全：绑定验证邮箱、使用 30 分钟一次性找回链接、绑定 TOTP 并完成第二步登录、保存/使用一次性恢复码、撤销设备会话。错误、过期和重放凭证必须拒绝；密码找回不能关闭 MFA，撤销后的设备不得继续访问。
- 文本流式：OpenAI/Gemini 分段输出先持久化片段，再生成唯一正式消息并结算；继续验收工作台与画布显示、刷新续读、本地停止与服务端取消，以及切换账号后旧片段不得写入新会话。
- 分组与报价：模型列表、报价、创建、重试和执行均执行分组权限，核对预冻结金额与最终实付；周期赠送和消费上限已移除，已到账余额保留。

- 公网配置文件：服务器使用 `/opt/infinite-canvas-public/.env`，其中包含独立数据库、Redis、MinIO、对象桶、加密密钥和 `infinite_canvas_public_session`；该文件不得提交 Git。
- 用户通知：密码、邮箱、两步验证和新设备登录，以及充值、公益额度、签到到账和当日余额偏低会出现在通知中心；已验证邮箱且 SMTP 启用时同步发信。同一设备在仍有匹配会话时不会重复提示登录。
- 渠道运营补充：一个渠道可检测多个已绑定模型，管理员可提交全部已配置渠道，模型没有任何可用渠道时每小时最多通知管理员一次。
- 成本界面：可按时间、来源、模型和渠道筛选，导出当前页，并跳到未知成本。清理保留成本账和成本核对审计；检测记录 30 天、通知 90 天、其他审计 365 天。
- 钱包与状态：钱包展示本月实付/到账、明细筛选和 CSV；`/user/stats` 同步本月汇总；`/admin/status` 展示队列、邮件积压、渠道冷却和存储依赖。
- 成本账务：模型渠道独立配置人民币成本，区分未知、配置估算与人工核对；清理请求日志后保留成本，上游成功但结果保存失败时仍有成本记录；检查实付、已知成本及公益支出。音频按秒计价依赖 ffprobe，Go Dockerfile 已声明安装。
- 敏感词：仅记录规则放行并保留命中事件，不记录完整提示词；拦截规则仍拒绝生成。检查最近命中表和管理审计。
- 此前验证范围：扩展前曾通过 30 组后端测试与 52 项前端回归，本次扩展的验证结果见本节开头；此前本机已使用独立 `.env.local` 完成 Docker 前端/API 镜像构建并启动，API、PostgreSQL、Redis、MinIO 健康检查以及 `/health`、首页、登录页均正常。生产 `.env.public`、公网 HTTPS 反代、真实 SMTP/上游/商户支付、对象存储备份恢复和目标硬件压测仍需验收；配置步骤见仓库 `docs/public-platform.md`。

### 前期 Go 平台基线

- 独立 PostgreSQL、Redis、MinIO 上累计 14 项后端测试通过：精确金额、并发充值回调、随机签到、邀请码名额、整批余额回滚、跨实例渠道槽位、取消与迟到结果、文本历史、视频原任务续查、媒体顺序/权限、SSRF、登录频控退款、管理员密码重置和公告版本。
- 浏览器已查看登录、四类创作工作台、钱包、支付配置和运营设置页面；继续验证邀请注册、昵称与账号安全流程、不同窗口登录退出、浅深色主题与手机布局。
- 已在隔离环境（模拟上游渠道）跑通端到端闭环：四类生成任务提交、冻结、结算、取消退款均正确（测试余额 ¥10 → ¥9.60），图片/视频/音频可保存为素材，插入画布后刷新可恢复三种媒体节点。
- 创作工作台图片页与视频页的尺寸记忆相互独立，直接打开视频页不再带入图片尺寸；批次任务全部落定后自动刷新钱包与账本缓存，冻结余额变化即时反映。
- 新增运营能力待验收：用户分组折扣（后台建组、用户列表调整分组，生成/对话价格按折扣换算）、充值兑换码（后台创建、钱包页兑换、每码每用户限一次）、视频/音频按秒计价（模型配置每秒价格后按请求秒数冻结与结算）、渠道余额查询（渠道列表“余额”按钮，走 dashboard/billing 端点，不支持的渠道会报错）、敏感词拦截（后台维护词库，生成与文本提交按子串拦截，review 动作已改为人工审核，log 动作仅记录）。数据库迁移支持 v2→v3 原地升级，升级前请照常备份。
- 以管理员配置图片、视频、文本、音频的真实 OpenAI/Gemini 兼容渠道，验证参数、参考资源、模型按结果收费、生成失败退款、后台停用渠道和任务取消。
- 文本模型 token 级计价：管理员给文本模型配置输入/缓存命中/输出每百万 token 单价后，提交时按历史长度与 max_tokens 预估冻结，完成时按上游 usage（OpenAI prompt_tokens_details.cached_tokens、Gemini cachedContentTokenCount）实际用量结算并多退少补；上游缺 usage 时回退按冻结额结算。已用隔离测试覆盖缓存计价与冻结清零。
- 验证画布刷新后图片/文本恢复及视频/音频续查，媒体下载、保存到素材与导入导出；删除生成历史后其他画布或素材引用应继续有效。
- 配置随机签到区间，测试北京时间跨日、同日重复点击和多个窗口并发领取；使用独立测试账户检查所有余额变动与账本对应。
- 完成易支付经典接口、支付宝官方当面付、微信 Native API v3 的真实商户联调：金额/商户不匹配拒绝、重复通知幂等、未支付查单关闭、迟到支付入账、密钥更换后旧单处理。
- 在目标服务器验证 1Panel -> Web Nginx -> Go API 的 HTTPS、Cookie、代理 IP、支付通知和静态资源路径；测试多实例扩容、Redis/数据库/MinIO 故障、备份恢复及持续压力下的延迟和成功率。
- 前端生产构建、Docker 镜像构建与真实支付未执行，生产吞吐量尚未验收。配置和启动步骤见仓库 `docs/public-platform.md`。

## 公司版基线记录

- 故障转移专项：隔离数据库与模拟上游的 30 项用例通过。排队后应重新检查渠道与模型绑定状态，故障冷却先于槽位释放，迟到成功不得清除较新的冷却；明确审核拒绝（含 HTTP 200 拒绝字段）应停止切换且不误冷却渠道，普通 503 安全服务故障应正常切换；健康和尝试记录写库失败不得增加模型调用，存储/结果落库失败不得记录成功费用。现有超时、冷却时长、并发上限和模糊错误最多额外切换一次的规则保持不变；真实渠道联调仍待验收。
- 登录后页面复核：画布、版本历史、生图工作台、素材、提示词、个人中心四页及管理后台六页及其主要读取接口已验证正常；画布生成配置仅显示图片/文本、个人中心退出按钮显示中文、零存储量显示 `0 B` 均已在更新后的本机镜像中复核。当前本机未发布文本模型且 Agent 未连接，实际 AI 生成与 Agent 工具调用仍需单独验收；两张第三方提示词示例图存在外部资源拦截。
- 公网 HTTPS 安全复核：1Panel -> Web Nginx -> API，3000/3001 不对公网开放；生产登录 Cookie 应包含 Secure，伪造代理头前缀不应绕过登录限流，非受信浏览器来源的写请求应被拒绝。部署环境已有 `COOKIE_SECURE=false` 时需显式改为 true。
- 账号隔离：A 的画布详情、素材保存、模型列表、上传或生成请求尚未完成时切换 B，B 不得看到 A 的私人数据、系统提示词、失败回滚、查询缓存或 Agent 事件；剪贴板读取、画布压缩包导入、参考图读取、裁剪/切图/放大和视频截帧完成后，也不得使用新会话上传或提交旧内容。旧上传返回 401 不得登出新账号，其他共享 Cookie 的标签页应失效并清理。
- 文本恢复与幂等：首次生成、多文本和重试应保留各槽位的请求与对话 ID；提交后响应断线仍须按服务端状态恢复成功或失败，并保留已成功的其他槽位，迟到响应不得覆盖已恢复结果；提交前失败则直接显示失败，重试准备中刷新不得把旧失败请求误当成等待中的任务。同一失败请求（含 UUID 大小写不同）并发重试只调用一次上游，成功重放返回完整 ID 且不受当前渠道状态影响；后续追问应按原顺序保留历史图片，系统提示词通过 system 角色传递。
- 画布历史图片：迁移 `0010_canvas_history_media` 为仍存在的服务端历史补齐图片引用；删除当前节点、撤下公共素材、恢复最旧保留版本、裁剪历史及删除项目，应正确保留或释放文件。MinIO 删除抛错或部分失败时必须保留元数据供后续重试。
- UUID 并发互斥：对同一画布交替使用大小写不同的 UUID 并发保存，媒体引用须只对应最终快照和保留历史；失败图片任务正在重试时，以大写批次 UUID 删除也须返回进行中，不能删除已重新排队的任务。
- 上游错误脱敏：错误 JSON 用 Unicode 转义、斜杠转义或长文本回显渠道密钥时，用户错误提示和请求日志均不得出现解码后的完整密钥。
- 版本操作与后台生成：创建或恢复快照前应等待保存队列完成，保存失败时中止操作；图片前台等待结束后，仍在服务器执行的任务不得被标成终态失败，后台应继续回填最终结果。
- 可选本地 Codex：Agent 启动链接的片段参数应跨登录和强制改密保留；返回同一账号时保留其本地连接配置，但读取附件、画布引用或消息预览期间重新登录后，不得继续发送旧消息或恢复旧附件。添加素材须等待服务端成功，生图工作台须在偏好加载后使用 Agent 指定的模型和参数。平台文本模型保持独立，视频/音频工具不得触发未接入的生成接口。

- 上游 v0.18.0 同步与对齐：多选节点虚线选区框选交互与一键打组 / 解散组；视频生成引入 `videoTaskId` 支持重新进入画布后的后台任务断点轮询恢复；生图设置按宽高比与 1K/2K/4K 规格对齐，Gemini 采用 `imageConfig` 协议生成；多图参考协议与本地代理转发整合。
- 渠道故障冷却期与自动自愈：管理员可在「渠道管理」为每个渠道独立配置冷却时长 `cooldownSeconds`（0~86400 秒，默认 120 秒），调度器动态采用该配置进行熔断；401/403 异常冷却 5 分钟或按配置冷却后自动恢复调度探测，避免渠道永久失效。
- 文本请求失败后重试：同一 `requestId` 的文本请求失败后再次提交，应原地复用该请求记录重新执行（状态回到运行中、清空错误码与响应关联），不再返回「数据已存在」冲突。
- 本机 Docker 构建与启动已验证：前后端镜像构建通过，四个服务运行，API、PostgreSQL、MinIO 健康检查通过；Web `/health` 与登录页返回 200，浏览器登录页无运行错误，11 条迁移已应用至 `0010_canvas_history_media`。两层代理信任改用 Fastify 支持的函数形式，原有代理边界与限流用例通过；已使用现有账号的登录会话复核页面与主要读取接口。

- Gemini 请求头鉴权与 NewAPI 兼容：Gemini 协议上游调用与渠道连通性测试改用 `x-goog-api-key` 请求头传递密钥（针对以 `sk-` 开头的 NewAPI 令牌同时兼容 `Authorization: Bearer`），彻底移除 URL query string 中的 `key` 参数防止泄露至反代日志；同时对 Gemini 的 `baseUrl` 自动做 `/v1beta` 路径补全容错。
- 私有媒体鉴权与 ETag 协商：`GET /api/media/:id` 的 200/304 响应均携带 `Cache-Control: private, no-cache` 与 ETag；命中 ETag 仍须先检查当前用户权限，切换无权账号后应返回 403。升级后强制刷新旧页面一次，使原有一年缓存重新获得鉴权策略。
- 媒体批量删除与 MinIO 往返优化：`removeUnreferencedMedia` 改为单事务批量标记 `deleting`、批量引用核验与单批次 MinIO `removeObjects` 删除，消除单批次 20 张图 40 次串行网络往返。
- 渠道调度行锁争抢优化：`markChannelResult` 仅在渠道先前异常/冷却需要恢复时落库更新，常态健康调用不更新 `channels` 表，消除高并发下所有 worker 在同一行上的排他行锁排队等待。
- 登录防爆破双维度限流与内存硬上限：新增纯 IP 失败计数限流（5 分钟 30 次），与单账户限流（1 分钟 5 次）形成双重拦截；`SimpleRateLimiter` 达到 5000 容量时采用 LRU 淘汰最旧条目，杜绝内存无界膨胀。
- 基础安全响应头与用户批次排队上限：为全部 API 响应注入 `X-Content-Type-Options`、`X-Frame-Options` 与 `Referrer-Policy`；`POST /api/generation-batches` 增加单用户未完成任务上限拦截（最多 50 个排队/进行中任务）。
- 文本请求幂等重试与文案动态化：针对 `failed` 状态的请求，相同 `requestId` 重试时重置状态重新发起调用，不再触发主键冲突导致 409；附件大小限制报错文案动态读取 `MAX_UPLOAD_BYTES`。
- 路由按需加载：`/admin/*`（6 个页面）、`/user/*`（4 个页面）、`/canvas/:id` 与 `/image` 改为独立 chunk，普通用户首屏不再下载管理后台代码；首次进入这些页面时侧边栏与顶栏应立即可见，仅内容区显示居中加载动画，加载完成后功能与之前一致；未登录跳转、`AdminGuard` 权限拦截和 `/user`、`/admin` 的默认重定向行为不应变化。
- 管理端用量统计并发查询：`GET /api/admin/stats` 的 7 条分析查询（队列、存储、文本汇总、总计、按用户/模型/渠道）改为并发执行，返回结构与数值应与之前完全一致，大时间范围下响应明显更快。
- 新增性能索引（`0008_perf_indexes`）：`media_objects_orphan_idx` 为孤儿媒体清理的部分索引（`status='ready' and reference_count=0`），每日清理不再全表扫描；`assets_updated_pagination_idx` 覆盖素材列表的 `updated_at desc, id desc` 排序，素材分页翻页更快。执行迁移后素材列表顺序与孤儿清理行为应保持不变。

- 画布保存链路可靠性：新增 `0007_canvas_project_history` 迁移，全新部署执行迁移后画布保存不应再因缺表失败；Fastify 请求体上限对齐 `MAX_UPLOAD_BYTES`，大画布（快照超过 1MB）应能正常保存，超限时返回 413 而不是 500；保存失败改为最多 5 次指数退避重试（2s 起，上限 30s），命中 4xx（如 413/400）立即停止自动重试，画布顶栏出现红色提示并可点击手动重试，待写入的编辑内容不会丢失。
- 画布项目列表与快照按需加载：`GET /api/canvas-projects` 只返回标题、时间与节点/连线数量，项目卡片统计仍应正确；进入某个画布时才拉取该项目详情快照，快照未加载完成前不得触发保存（不应把画布覆盖成空）；从历史版本还原后只同步本地状态，不再额外回写一次。
- 画布自动版本备份限频：连续编辑时自动备份仅在「忽略视口后内容确实变化」且距上次自动备份超过 5 分钟时才写一份快照；只拖动或缩放视口不应产生新的历史记录；手动创建快照与历史还原仍应每次都备份当前版本，历史记录上限保持 20 条。
- 个人中心账号操作修复：修改显示昵称应成功并同步顶部导航显示；修改密码应成功并要求至少 10 位（与服务端一致），成功后自动登出并跳转登录页。
- 生图历史分页与轮询：`/user/generations` 改为服务端分页（每页 50 条），不再一次拉取全部历史；仅当当页存在排队/运行中的任务时才按 3 秒轮询，标签页切到后台时自动暂停；「整批转存」按实际成功数量提示并保留失败项为未转存；「打包下载」应产出可正常解压、文件名正确的压缩包。
- 素材列表分页：`GET /api/assets` 支持 `limit`/`offset` 并按 `updatedAt` + `id` 稳定排序，前端分页拉取最多 1000 条，公共素材较多时不应一次性下发全站数据。
- 生成图片下载安全校验：上游返回的图片地址若解析到私网、回环、链路本地（含 `169.254.169.254`）、CGNAT 或组播地址应拒绝下载并记为失败；302 跳转逐跳校验，最多跟随 4 跳；中转网关部署在内网时可通过 `ALLOW_PRIVATE_IMAGE_HOSTS=true` 放开。
- 同步文本生成超时：Nginx 对 `/api/` 的读写超时放宽到 600s，长耗时文本生成不应在 60 秒被网关切断为 504。
- 会话消息顺序：同一事务内写入的多条消息（时间戳相同）在对话详情与上下文窗口中顺序应稳定正确。
- 参考图复用与资源清理：同一批次多张图共用参考图时，参考图只从 MinIO 读取一次并在批次内共享，多图批次的内存占用与对象存储请求数明显下降；过期会话每天定时物理删除，`sessions` 表不再持续增长；请求日志过期清理只统计影响行数，不再把 30 天日志主键全部读进内存。

- 画布版本历史与手动快照管理：画布左上角菜单上线「版本历史」入口，支持用户输入备注手动创建快照、浏览历史版本列表（区分自动备份与手动快照、节点数量、连线数量与时间戳），并支持一键将当前画布秒级还原至指定历史版本；前端在 `useCanvasStore.hydrated === false` 时强制禁止发送 `PUT /api/canvas-projects` 保存以杜绝空状态覆盖，服务端自动保留最近 20 版历史记录。
- 媒体上传与生成文件大小上限提升：Nginx、Fastify 与配置环境变量默认将单文件上传上限（`MAX_UPLOAD_BYTES`）及生成图片上限（`MAX_GENERATED_BYTES`）由 20MB 调整为 50MB，支持导入 20MB 以上的高清/大体积图片素材，并在超限时准确返回统一错误。
- NewAPI 多图生图与画布视觉排序修复：OpenAI 协议 `/images/edits` 接口参考图改用标准同名字段 `image`（多次重复传递），兼容 NewAPI/OneAPI 等网关的多图解析；画布收集连接资源时按视觉坐标（$y$ 坐标升序优先，其次 $x$ 坐标）稳定排序，确保用户眼中的图1/图2与系统序号严格一致；多图提示词前缀补充别名映射（支持图1/图2/Image 1），避免模型引用冲突。
- 生图 Worker 真并发消费：单 Worker 批量拉取重构为多独立 Worker 实例（按配置并发数并行监听），彻底消除单个上游长请求阻塞整个队列拉取循环的问题，提交多任务时所有空闲槽位立即并行执行。
- 登录限流器内存清理：登录与改密滑动窗口限流器支持每 10 分钟自动扫描清理过期记录，条目超限时触发容量主动淘汰，长周期运行下无内存泄漏。
- 媒体流式下载与内存回收：生成图片下载与 MinIO 参考图/附件读取在流超限或异常时主动执行 `reader.cancel()` 与 `stream.destroy()`，显式切断连接并回收内存，避免堆积占用。
- 画布保存防抖与卸载即时落库：画布常规修改防抖调整为 1000ms 降低数据库锁开销；在切换路由或关闭浏览器标签页时自动触发 `flushProject` 即时保存最新快照。
- 渠道并发与稳定性修复：渠道并发槽位改用应用层内存信号量，在上游请求（长达 8 分钟）期间零占用数据库物理连接，彻底杜绝连接池耗尽与死锁；pg-boss 任务超时延长至 3600 秒且采用标准模式，避免故障转移被误判超时。
- 账号安全与防爆破：登录与改密接口对单 IP 强制 5 次/分钟滑动窗口限流，用户不存在时执行哑哈希校验以拉平响应耗时，消除用户名枚举侧信道；管理员路由前缀增加强制角色校验。
- 文本幂等与中断回收：携带重复 `requestId` 的文本生成请求直接复用响应；服务端重启后中断的文本请求及其关联的 `request_logs` 均同步回收为失败。
- 媒体孤儿清理与权限隔离：孤儿媒体清理严格执行“先删 DB 记录再调用 MinIO 物理删除”的安全顺序；严格限制跨用户媒体引用。
- 前端轮询与素材保存修复：画布恢复轮询自动排除前端当前活跃生成节点，消除双重轮询；轮询支持 AbortSignal 立即中断；加入素材改为 Promise 异步并在异常时准确捕获提示。
- 模型渠道未就绪提示：画布和工作台在未配置可用模型渠道时统一引导“请联系管理员在后台配置渠道”。
- 公司内部平台账号与权限：使用引导管理员创建账号后，新用户首次登录应强制修改临时密码；管理员应能创建账号、禁用账号和重置密码，禁用或重置后该用户的现有会话应立即失效；普通用户不得进入 `/admin`，也不得从模型选择器或接口读取渠道名称、Base URL、API Key、优先级、权重和图片单价。
- 公司平台跨设备数据：同一账号在两台设备通过公网 HTTPS 域名登录后，应看到一致的画布项目、节点、连接、视口、助手消息、私人/公共素材、图片生成批次和文本结果；画布连续编辑应在现有防抖后串行保存，刷新或短暂保存失败后不得用旧响应覆盖新快照；同一画布同时编辑采用后保存覆盖前保存的已知限制。
- 公司内部平台素材权限：所有用户都应能添加公共素材；创建者可编辑和删除自己的公共素材，管理员可管理全部公共素材，其他普通用户只能使用；删除公共素材条目后，已加入其他用户画布或私人素材的图片仍应可读取，私人素材和其他用户数据不得越权访问。
- 公司内部平台文件存储：画布上传、拖放、素材和文本附件仅接受 PNG、JPEG、WebP，单张用户图片超过 20MB 应拒绝；上游生成结果超过 50MB 或不是受支持图片时应失败且不计费；上传和生成图片应记录宽高，刷新后由受权限保护的 `/api/media/:id` 从 MinIO 恢复，不把大体积 base64 作为权威数据。
- 公司内部平台图片任务：一次生成多张图片时应只创建一个批次，并在其中拆成独立单图任务显示排队、运行、成功和失败；关闭或刷新页面后任务继续且重新登录可恢复，点击画布「停止」应明确只停止页面等待，服务端继续执行和计费并在完成后自动回填；失败槽位应将原子任务重新排队且不新增批次，已完成且非运行中的批次可删除，全部历史批次均可加载，仍被画布、素材、其他批次或消息引用的图片不得从 MinIO 删除。
- 公司内部平台图片任务恢复：刷新或停止页面等待后，带服务端批次标记的图片子节点及其配置父节点应继续保持正确的排队、运行、成功和失败状态。
- 公司内部平台渠道调度：同一公开模型配置不同优先级时应严格主备，同优先级多个渠道应按权重分流；网络失败、超时、429、5xx、401 和 403 应按规则切换，用户参数或内容审核拒绝不应自动切换，模糊错误最多额外切换一次；全局图片并发上限和单渠道并发上限均应生效，刷新页面不得中断服务端任务。
- 公司内部平台文本链路：画布文本生成与图片问答应只使用管理员发布的文本模型，节点应保存并复用服务端 `conversationId`，并用 `textRequestId` 精确关联当前请求；消息、模型、实际渠道、状态和耗时应在服务端持久化并按画布归属；当前请求的 PNG/JPEG/WebP 附件应作为 OpenAI 或 Gemini 多模态输入，历史消息不得错误重复附带本次图片；重复提交同一请求 ID 不得产生幽灵对话、重复消息或附件引用，多文本并发中的每个子节点应独立结算，刷新或停止页面等待后应恢复当前请求及父配置节点状态，API 重启后中断的文本请求应转为失败而不是永久停留在运行中。
- 公司内部平台请求日志：管理后台新增「请求日志」页，生图、文本以及渠道「测试/获取模型列表」的每次上游尝试（含失败重试）都应生成一条日志，展示时间与状态、用户、类型、平台模型/渠道/上游模型、HTTP 状态、费用、耗时和错误详情；支持按时间范围、用户、模型、渠道、类型和状态筛选并服务端分页；点击详情列应复制完整请求信息；有运行中记录时列表应自动刷新；超过 30 天的历史日志应自动清理，「清空日志」只删除历史记录且需二次确认。
- 公司内部平台统计与后台：管理员应能维护公开模型、渠道、上游模型映射、优先级、权重、超时、渠道并发和元/张单价，并能从兼容接口获取模型列表；统计页应支持今天、近 7 天、近 30 天和自定义范围，按用户、模型和实际渠道筛选图片数量、费用、耗时、两种成功率及文本调用，并显示当前图片队列积压；按实际渠道筛选时仍须计入该渠道失败及切换前的尝试；渠道页应显示最近尝试状态、耗时、上游模型和错误摘要；管理员公共素材页应支持筛选、搜索、编辑和删除。
- 公司内部平台用量统计修复：其他用户成功生成图片后，统计页应显示对应请求数、成功图片数、费用及用户/模型/渠道明细；重新进入或聚焦统计页时应获取最新数据，页面内容区左右留白应与其他管理页一致。
- 公司内部平台用量统计布局：桌面端 10 个指标卡片应压缩为每排 5 个、共两排，并明显减少单卡片高度；窄屏下仍应保持可读和自适应换行。
- 公司内部平台管理布局：桌面宽度下，模型管理和渠道管理应使用主内容区的可用宽度，避免外围出现过大的空白；窄屏下表格、操作、抽屉和弹窗仍需保持可用。
- 公司内部平台模型配置：渠道表中的接口地址列应保持紧凑，渠道与接口地址列收窄、最近尝试列加宽；最近尝试应完整展示错误信息与开始时间（不截断），点击该单元格应复制包含状态、耗时、上游模型、错误和时间的完整详情。管理员获取上游模型后应能搜索并直接创建平台模型或绑定已有模型，在同一流程中配置能力、价格、发布状态、优先级、权重和启用状态。
- 公司内部平台图片模型排序：管理员编辑模型时设置的排序值应持久化，数值越小越靠前；普通用户生图工作台和画布模型选择器应按该顺序展示，排序值相同时按显示名称稳定排序。
- 公司内部平台模型变体：两个显示名称和价格不同、但都绑定同一上游标识（例如 `gpt-image-2`）的公开模型应能同时保存；普通用户模型选择器应能独立选择这两个变体。
- 公司内部平台模型删除：删除模型后应从管理端和普通用户模型列表隐藏，清理渠道绑定，同时保留历史图片生成、文本请求和统计记录；删除后的模型不得再次被选择。
- Gemini 图片渠道：配置 `https://torchai.ai/v1beta` 等 Gemini `generateContent` 中转地址后，生成请求应携带 `?key=` API Key，并使用 `generationConfig.imageConfig` 和 `responseModalities`；中转站映射的 Nano Banana 图片模型应能正常返回图片，不应因错误的 `responseFormat` 字段被判定为参数错误。
- Gemini 安全审核响应：上游返回 HTTP 451 或不安全提示词时，应明确提示“内容审核拒绝”，指导用户修改提示词或参考图，并且不应切换到备用渠道。
- 模型选择器图标：平台模型按 UUID 选择时，应根据显示名称渲染供应商图标；GPT Image 显示 OpenAI 图标，Banana/Nano Banana/Imagen 显示 Gemini 图标，只有未知模型使用 CPU 兜底图标。
- 本地 Agent 恢复：登录后的普通工作台页面应能从顶部导航打开可调整宽度的 Agent 面板，画布页面应显示 Agent 按钮和紧凑连接状态，浏览器已有本地 Token 时应静默重连；Agent URL、Token、Codex 模型、技能、历史、日志和画布操作仍只属于浏览器与本地 Agent，不得写入平台渠道配置；管理后台不得挂载 Agent 面板。
- 公司平台部署与恢复：Docker Compose 应启动 Web、API、PostgreSQL 和 MinIO 四个服务，公网只通过 1Panel 的 HTTPS 域名访问，3000/3001、数据库和 MinIO 不向公网开放；需实际验收登录、Secure Cookie、反向代理和健康检查。PostgreSQL 与 MinIO 必须按同一恢复点备份和恢复，加密主密钥需要独立备份且不得写入日志。
- Docker API 队列恢复：已有 `generate-image` 队列策略不是 `short` 时，重启 API 应原地更新策略并保留队列分区及任务，容器应能通过健康检查，不应因外键约束错误反复重启。
- 公司内部平台图片编辑重点项：普通参考图编辑应继续可用；遮罩编辑当前由平台作为附加参考图交给上游，需要重点验证目标渠道是否符合预期遮罩语义、生成结果和错误提示是否可接受。
- 渠道调度性能优化：高并发占用渠道槽位时应能正常排队等待（最长约渠道超时一半时间后切换备用渠道），单渠道并发上限仍应准确生效；创建生成/文本请求在渠道全部冷却时仍应正确提示「暂无可用渠道」，且该检查不再解密密钥。
- 文本链路截断与列表：长对话超过 50 条消息后，新请求只携带最近 50 条历史，较早的上下文不再发送给上游；会话列表接口改为最新更新在前并支持 `limit`/`offset` 参数，默认返回 100 条。
- 上游超时与大小提示：上游响应体下载缓慢或卡住时应在渠道超时后失败并可切换渠道，而不是无限等待；把 `MAX_GENERATED_BYTES` 改为非 50MB 的值后，超限错误文案应显示实际配置大小。
- 前端批次轮询与模型缓存：图片批次长时间无进展时前端最多等待 10 分钟并提示稍后在生成记录查看，轮询间隔从 700ms 逐步放宽到 3s；连续多次生图/文本请求期间 `/api/models` 在 60 秒内只应请求一次。
- 统计与队列稳定性：按用户/模型筛选统计在大数据量下仍能较快返回；API 重启恢复排队任务时不产生重复投递的空跑任务（可通过重启前后 pgboss 任务数验证）；批量创建 20 张图片任务的入队耗时应明显低于串行。
- 工作台多批次并行：一个批次运行中时应能直接提交新批次、点击查看其他记录而不被打断；左侧生成记录显示每个进行中批次的实时进度，页面首次加载只发一次列表请求（含每批次成功/失败/缩略图摘要）。
- 完成通知与批量操作：切换到其他标签页后批次完成应弹出系统通知并更新标签页标题为「✓ 图片生成完成」，回到页面后恢复；结果区多张失败时出现「重试失败项 (n)」一键重试；多张成功时出现「打包下载」，zip 内按序号命名。
- 下载命名与引导：单张下载文件名应包含提示词前缀、日期时间和序号；模型列表为空时模型选择处显示联系管理员的引导文案；渠道全部冷却时创建请求的错误提示包含「请联系管理员」。
- 渠道测试与凭据交接：渠道管理新增「测试」按钮应返回上游模型数量且不打开配置抽屉；401/403 触发冷却的渠道在状态列显示「冷却中」标签；创建用户或重置密码后应弹出一性次凭据卡（账号+临时密码+复制按钮），关闭后不再显示。
- 角色与公告：管理员可将普通用户改为管理员或反向调整（不能调整自己），角色列筛选和状态筛选生效；平台管理右上角可发布公告，普通用户工作台顶部出现黄色公告条并可点击「知道了」关闭（本次会话内不再出现）；统计三张表支持点击表头排序和导出 CSV，有排队任务时每 5 秒自动刷新。
- 存储占用与孤儿清理：账号菜单应显示「我的占用：N 张图片 · 大小」且与管理员统计页「对象存储」卡片数值一致；上传参考图后不提交生成，超过宽限期（默认 7 天）后该文件应在服务端日志无报错的情况下被自动清理；被画布、素材、批次或消息引用的文件不会被清理。
- 本地代理：配置弹窗应在「渠道」和「偏好设置」之间出现独立的「本地代理」页签，开关默认关闭。打开后应显示 `npx @basketikun/canvas-proxy@latest` 启动命令（端口非默认时自动追加 `--port`）、复制按钮，以及默认 `http://127.0.0.1:23210` 的地址输入框，失焦时自动补全协议并去掉末尾斜杠；「测试连接」应只在命令已启动时成功。开启代理后，拉取模型列表、生图、生视频、生成文本（含流式输出）、生成音频、自定义调用脚本、视频任务轮询、生成结果图片和视频的下载，以及 WebDAV 测试连接与同步都应经代理转发且功能正常，关闭后恢复直连。代理本身应原样转发请求方法、请求头、查询参数和请求体，SSE 响应按块透传不缓冲，`data:` 和 `blob:` 地址不受影响。
- 图像与视频尺寸设置：图像设置应将分辨率（`1k` / `2k` / `4k` / 自动）和宽高比（`1:1`、`2:3`、`3:2`、`4:3`、`3:4`、`16:9`、`9:16`、`21:9`、`9:21`、自动）拆成两项，并由二者计算 W/H。视频设置仍使用 `480p` / `720p` / `1080p`（以及自定义 p 值），比例为 `1:1`、`3:4`、`4:3`、`16:9`、`9:16`、`21:9`、自动，并由清晰度与比例计算 W/H。秒数应为 4–30 的滑杆，右侧显示当前秒数；比例为自动时应禁用 W/H。
- 视频内置请求：未配置调用脚本时，OpenAI 格式应按模板发送 `mode`、`first_frame`/`last_frame` 或 `image[]`、`generate_audio`、`watermark` 以及参考视频/音频，不再使用 `input_reference[]`；Gemini 格式应走 `predictLongRunning`，按模式发送 `image`/`lastFrame` 或 `referenceImages`，不再提示不支持视频。刷新后应能继续查询 Gemini 任务状态。
- 视频生成模式：视频设置应出现「首尾帧」和「全能参考」；1 张图按首帧、2 张图按首尾帧发送专门字段，超过 2 张即使选了首尾帧也按全能参考发送。内置请求和调用脚本的 `params.mode` 均为 `frames` 或 `reference`。
- Gemini 生图：Gemini 格式渠道和 Gemini 生图调用脚本应使用 `generationConfig.responseModalities: ["TEXT", "IMAGE"]`，比例和尺寸放在 `imageConfig.aspectRatio` / `imageConfig.imageSize`，不再发送 `responseFormat`。
- 视频调用脚本：能力为「视频」的模型打开「调用脚本」后应进入全屏分步配置；左侧按「看懂规则 → 让外部 AI 写 → 可用变量」展示，可用变量应包含 `videos`（参考视频 File 数组）和 `audios`（参考音频 File 数组）；「复制写脚本说明」应包含当前能力、可用变量、返回要求和写法约束。插入的模板应是带参数列表的 async function，并把 `params` 拆开；OpenAI 生图模板必须包含 `quality`、`size`、`count`、`background`。OpenAI 视频模板应使用 FormData，带上 `mode`、时长、尺寸、分辨率、配音、水印以及参考图/视频/音频；`mode=frames` 时应使用 `first_frame` / `last_frame`，`mode=reference` 时应使用 `image[]`，完成后轮询并下载 `/content`。把外部 AI 写好的脚本粘贴到右侧并保存后，画布连接参考视频或参考音频再生成时，脚本应能读取这些文件。
- URL 凭据导入：使用新的有效 `baseUrl` 和 `apiKey` 打开应用时，应新增独立渠道且不修改已有渠道；再次导入相同 Base URL（含末尾斜杠或 `/v1`）时只更新对应渠道并保持幂等；仅提供 API Key 或提供非法 Base URL 时，所有配置都应保持不变。
- 文档站导航与本页目录：手机宽度打开文档侧边栏时，顶部应显示当前文档分类及说明；展开后可在项目介绍、操作手册、开发文档、项目进度、商务合作和赞助支持之间切换，切换后分类标题及下方页面列表应同步更新；桌面端顶部分类栏保持不变，宽度达到 960px 时正文右侧应显示带层级折线的本页标题目录，点击可跳转，滚动时自动高亮当前标题；较窄宽度继续显示顶部折叠目录。
- Canvas Agent 生成流程引用：提示词仅包含已经通过 `referenceNodeIds` 传入的 `@[node:...]` 引用时，应直接把现有文本、图片、视频、音频或组节点连接到生成配置节点，不再创建重复的提示词文本节点；提示词包含普通文字时仍应创建独立的提示词文本节点。
- 视频画面截取：右键有内容的视频节点应显示「截取首帧」「截取尾帧」「截取当前帧」；三项操作应按视频原始比例在右侧生成并选中图片节点，同时建立视频到图片的连线；「截取当前帧」应使用播放器此刻停留的画面，截取过程不得改变原视频的播放位置。
- 文本节点：节点正文右上角不再显示「生图」按钮，文字内容应使用完整正文区域；节点上方工具条中的生图入口仍可正常创建生成配置。
- 多文本生成：生成次数大于 1 时只创建一个文本结果节点和一条来源连线；节点右上角显示实际生成成功的文本数量，点击后应像多图节点一样展开所有备选文本；点击「设为主文本」应立即替换正文，原主文本保留为备选，双击正文编辑后再次切换回来仍应保留修改。
- 组节点引用：组节点应使用透明、轻量的边框与标题样式，并显示右侧输出连接点；把组连接到生成节点后，组内有内容的图片、视频、音频、文本及可引用扩展节点都应作为输入，普通提示词面板的 `@` 列表应显示组内资源，生成配置的 `@` 列表应允许一次选择整组；组与单节点重复引用时不得重复发送同一资源。
- 多选打组：框选或点选至少两个节点后，应出现包住全部选中节点的圆角虚线框，框上方显示「打组」；点击后用组节点包住这些内容并保持原位置，随后选中该组。选中组后可通过顶部「解散组」、右键菜单或 `Ctrl/Cmd+Shift+G` 取消分组；已同属一组的节点再次多选时应显示「解散组」而不是再打一组。拖入组、组引用和左侧树形列表应继续可用。
- 节点参考栏：普通节点提示词面板、放大编辑器和生成配置输入框上方应显示当前输入节点；图片和视频显示缩略图并可悬停预览，文本、音频及扩展节点显示对应类型；组连接应展开显示组内每个有效内容节点，删除任一展开项都应断开整组连线。点击加号进入画布选择后可连续添加有效参考，已连接、目标自身、已通过组引用的组内节点及不可引用节点不可重复选择，按 ESC 或顶部提示返回输入框；新增连接后 `@` 列表应立即更新。
- 多图透明背景：展开包含透明 PNG 或 WebP 的多图节点时，主图与所有备选图的透明区域都应直接显示画布背景；切换主图前后不得出现白色或主题面板底色。
- 画布选择与平移：左下工具栏应能切换「选择」与「移动」模式并用图标显示当前模式；选择模式拖动空白区域应显示疏朗清晰的虚线框，虚线长度、间距和粗细在任意缩放比例下保持一致；移动模式应能从空白区域或节点上拖动画布。按住 Control 或空格应临时反转当前工具且不改变工具栏模式，即选择变移动、移动变范围选择；点击画布页任意按钮后按空格应只临时切换工具，不得再次触发该按钮；输入框和可编辑文本内仍可正常输入空格。中键平移以及 Shift/Cmd 追加选择节点应继续可用。
- 文档站国际化：访问文档站首页及 `/docs` 应直接显示英文，不得出现重定向循环；切换简体中文后 URL 应带 `/zh-CN` 且首页、顶部导航、侧边栏、正文、搜索框和页面操作文案均显示中文；刷新、站内跳转和再次切回 English 后语言应保持正确，搜索结果与 Markdown/LLM 文档内容不得混入另一种语言。
- 国际化基础框架：从页面右上角切换简体中文或 English 后，首页、顶部及移动端导航、版本弹窗、配置主面板、渠道编辑、模型选择、页面标题、页面描述和 Ant Design 内置文案应立即切换，刷新页面后保持所选语言；偏好设置中不应再显示语言选项，WebDAV 同步错误、提示词来源及其他尚未接入翻译的业务模块暂时仍可能显示中文。
- 全局弹层主题：全站 Dropdown、Menu、Select、Cascader、TreeSelect 等弹层在浅色主题下应使用白色背景、轻灰悬停和选中态，在深色主题下应使用深色背景与低对比度浅色交互态，不应再出现默认黑灰色块、文字对比不足或单个页面配色不一致。
- 画布节点缩放稳定性：选中任意节点并反复拖动四角缩放时，节点工具条应在缩放期间隐藏、松开后恢复，页面不应再出现 `Maximum update depth exceeded`。
- Agent MCP 初始化状态：首次进入空白对话、点击「新对话」或恢复历史对话后，无需先发送消息，对话区应立即显示 Codex 会话准备，并列出 `codex_apps`、`infinite-canvas`、`node_repl` 等完整 MCP 服务状态；全部服务进入终态前可输入并保留草稿，但发送按钮、上传和回车提交应禁用；可选 MCP 失败时应提示警告但允许发送，`infinite-canvas` 缺失或失败时应阻止发送并显示明确原因；首轮及后续任务运行期间再次收到的 MCP 状态只进入诊断日志，模型开始处理及回复完成后对话区不应残留初始化卡片。
- Agent 模型设置：连接 Canvas Agent 后，输入框左下方应显示当前 Codex 模型与推理强度；模型列表应来自当前账号实际可用模型且不显示内部审查模型或重复项，切换模型后强度选项随模型能力更新且无空白选项，刷新页面后保留选择，发送任务时实际使用所选模型和强度；本地控制台与右侧「日志」应记录本轮使用的模型和推理强度。
- Agent 新对话响应：一轮对话完成后点击「新对话」，聊天内容应在服务端确认切换后清空并进入空白对话；后台应串行创建线程并预热 MCP，多个标签页应按同一会话版本同步，旧 HTTP 响应不得覆盖较新的 SSE 状态，初始化完成前不能发送到上一条会话或重复创建线程；重启 Agent 后应重新恢复并预热已有活动线程，无法恢复的失效空线程应自动替换为可用的新线程。
- Agent 读取画布卡片：读取当前画布完成后，卡片应按非零类型显示文本、图片、配置、视频、音频、分组、其他节点及连线数量，例如「3 个文本、5 张图片、2 个配置、4 条连线」；空画布应显示「当前画布为空」，执行失败时仍应显示错误信息，刷新恢复历史后统计保持一致。
- Agent 首次发送响应：在空白新对话中输入内容并按回车后，输入框应立即清空、用户消息应立即出现在对话中，再显示「正在思考」；线程创建或发送失败时，原输入和附件应恢复；任务运行期间输入的新草稿不应在请求成功后被清空。
- Agent 动态工具信息：执行内置生图、查看图片、命令、文件修改或其他动态工具时，卡片标题应显示具体工具名称；执行失败时正文应显示真实错误原因，刷新并恢复历史对话后仍应保持一致，不再统一显示「工具操作已完成」。
- Agent 画布生图：重启 Canvas Agent 后，其自动生成的工作区 `AGENTS.md` 应同步为最新版指令（用户自行编写的其他 `AGENTS.md` 不应被覆盖）；输入“生成一张图片”时应调用 `canvas_generate_image`，通过当前画布节点创建并运行生成流程；只有明确输入“使用 Codex 内置生图/ImageGen 技能生成”时才允许调用内置能力，Codex App Server 返回的 `imageGeneration` 结果应显示在对话中并作为保持原比例的图片节点添加到当前画布；Agent 不应在没有生成结果时提前声称“已生成”。
- Agent 顶部栏：在对话、历史和日志之间新增「技能」标签并显示已加载数量；连接状态应位于左侧 Agent 标题旁，并与右侧新对话使用相同规格的文字按钮，只有内容标签显示选中线；标签图标始终显示，中英文模式下都应按标签区域的实际可用宽度隐藏文字并保留小号数量，空间足够时显示图标和文字，拖动宽度时不应裁切、换行、横向滚动或错位。
- Agent 输入框窄屏布局：底部上传、Skill、确认模式、权限模式、模型和思考程度控件在窄面板下应只显示图标，宽面板下恢复文字；所有纯图标操作悬停时应使用统一样式显示名称或当前状态，任意宽度都不出现横向溢出。
- Agent 本地 Skill 管理：连接 Canvas Agent 后进入「技能」，应能读取当前 Codex 工作空间发现的项目、个人、系统和管理员 Skill，并支持搜索、按范围筛选、刷新及启停；未能加载的 Skill 应通过可点击、可滚动的详情查看原因；画布在站点工作空间 `.agents/skills` 下创建的 Skill 应可新建、编辑和删除，其他来源只读且不得被画布改写；编辑期间文件被其他窗口修改时应拒绝覆盖，清空显示名称、短说明和默认提示词后旧值应被删除，同时保留图标、品牌色等未由当前界面管理的元数据；多个标签页同时连接时，任一页面的 Skill 变更应自动同步，断开或切换 Agent 时旧请求不得重新写回列表或弹出编辑器。
- Agent Skill 草稿生成：创建 Skill 时应提供「空白创建」「从当前对话生成草稿」「从当前画布生成草稿」三种入口；空对话不得发起生成，取消时不得写入文件，确认后才写入站点工作空间 `.agents/skills`；Agent 运行中应禁用生成，多标签页下对话和画布来源必须分别严格绑定发起请求的 `threadId` 和 `clientId`，生成期间外部 MCP 仍应操作最近聚焦页面；草稿运行时创建、启停、删除和更新 Skill 应被统一拒绝，点击停止应真实中断临时草稿 turn；草稿不得泄露本地路径、密钥、媒体数据、真实节点 ID 或任务进度等临时状态，普通中文斜杠文本与 `/imagine` 等命令必须保持完整，生成草稿使用的临时 turn 不得出现在 Agent 对话、历史或日志中。
- Agent Skill 调用：在技能页点击「使用」或在输入框输入 `/` 搜索并选择 Skill 后，Skill 应作为原子 token 插入正文，而不是显示在输入框上方；发送任务时应通过 Codex 原生结构化 Skill 输入执行，发送成功后清除选择，失败时保留；新建对话、断开连接、Skill 被停用或删除时也应清除失效选择，发送前、任务完成后和刷新历史后的用户消息文字与 token 位置应保持一致。
- Agent 输入框快捷引用：输入 `/` 后应显示已启用 Skill 列表，输入 `@` 后应显示当前画布的图片、视频、音频和文本素材，长列表使用较高的内部滚动区域；选中项应按当前光标位置插入正文，例如「这是第一张图 `@图片1` 融合第二张图 `@图片2`」，不再集中显示在输入框上方，按 `Backspace` 或 `Delete` 时应整块删除；发送时应保持原句顺序，按节点 ID 精确引用发起页面的素材，并把引用图片的实际内容交给 Codex；输入框和聊天记录都应将引用渲染为与正文基线对齐的紧凑标签，悬浮或聚焦后可预览图片、视频、音频或文本内容，不重复显示内部图片附件，切换会话或刷新后引用样式应保持；可用方向键、`Enter` 或 `Tab` 选择，`Esc` 应关闭候选菜单，候选菜单打开时回车不得误发送消息。
- Agent 消息元数据：发送包含图片附件、Skill 或画布引用的消息后，刷新当前页面、在另一页面恢复同一对话或重启 Canvas Agent，历史消息都应由 Agent 按准确的 `clientMessageId`、`threadId` 和 `turnId` 恢复展示，本地缩略图仍可悬浮或点击查看，不依赖浏览器 IndexedDB 或文本匹配；通信协议版本变化不得清空独立的消息存储，遇到未知版本或损坏清单时应拒绝写入且保留原文件；删除线程后对应缩略图和引用元数据应同时删除，不得按记录数量或文件大小静默裁剪，改动前仅存在于浏览器旧缓存中的消息继续显示普通文本且不尝试迁移。
- Agent Markdown 样式：右侧 Agent 回复中的代码块应为横向占满消息区域、无语言标题和无双层边框的紧凑代码条，单行内容不应保留纵向大面积空白，复制操作仅在悬停时弱化显示；行内代码、链接在浅色和深色主题下应清晰；点击外部链接后应显示中文紧凑确认弹窗，长路径能够正常换行且复制、继续打开和关闭操作可用；点击 `/Users/`、`/home/` 等本地绝对文件路径时应改为提示在系统文件管理器中定位，支持复制路径，且浏览器地址不应跳转为 localhost 文件路径。
- Agent 工具确认模式：右侧面板标题栏不应再显示「工具确认」开关；对话输入框左下方应显示确认模式选择，默认选中「自动确认」，画布写入工具应直接执行；切换为「手动确认」后，画布写入工具应在对话中展示等待确认卡片，并支持批准或拒绝。
- Canvas Agent Codex 升级：启动 Canvas Agent 后应输出 Canvas Agent、内置 Codex 和本机 Codex 版本，实际运行的内置 Codex 应为 0.146.0；内置与本机版本不一致、未安装本机 Codex，或 npm 存在更新版本时应显示对应升级提醒，npm 检查失败不应阻止服务启动；Agent 应能正常连接 Codex、创建或恢复会话、发送消息并调用画布工具，运行中停止任务应只中断当前 turn，随后无需重启 Agent 即可继续发送新任务。
- Canvas Agent Debug：普通启动应通过公共 logger 输出 Info、Warn 和 Error 日志；使用 `npx -y @basketikun/canvas-agent@latest --debug` 启动后还应输出 Debug 日志并显示日志文件路径，终端统一采用“`YYYY-MM-DD HH:mm:ss` 级别 消息 详情”的纯文本单行格式，`~/.infinite-canvas/logs/` 下应按启动日期生成相同格式的 `canvas-agent-YYYY-MM-DD.log`，同一天多次启动应追加到同一文件；日志中不应出现连接 token 或图片 Data URL 原文。
- Canvas Agent 凭据脱敏：Codex 与 Claude CLI 的 stderr 包含 Bearer token、`sk-` 密钥、显式 API key/token/authorization 赋值、ANSI 样式或跨输出分片的凭据时，调试日志和网页诊断事件均不应出现明文；普通 stderr 文本应保持不变。
- Canvas Agent 配置权限：在 Linux 和 macOS 上保存配置后，`~/.infinite-canvas` 权限应为 `0700`，`canvas-agent.json` 权限应为 `0600`，已有的宽松权限也应被纠正；配置应能继续正常读写，Windows 启动不受影响。
- Canvas Agent URL 自动连接：链接应在 `#` 后传递 `agentUrl` 和 `agentToken`，进入或选择具体画布时继续保留，读取后立即从地址栏移除，并可连接有效的本机、局域网或远程 HTTP(S) Agent 地址且不主动展开设置面板；缺少参数或地址无效时应展开设置并显示错误。
- 图片 API 模型参数：`gpt-image-*` 的生成和编辑请求不应发送 `response_format`，`dall-e-*` 请求仍应发送 `response_format: "b64_json"`。
- 图片外链持久化：画布和生图工作台对图片接口返回的远程 URL 应只下载一次，经图片解码校验后保存到 IndexedDB；返回 `200` 但内容不是图片时应失败，下载期间停止画布生成后不得保存或应用结果；浏览器因 CORS 无法读取但图片仍可显示时，应保留原 URL 且不生成空 `storageKey`，插件调用在外链可读取时仍应收到 Data URL。
- 多参考图编辑：OpenAI 格式在多张参考图时应使用重复的 `image[]` 表单字段提交文件，单张参考图仍使用 `image`；新建的 OpenAI 图片脚本应使用相同字段名。Gemini 渠道和已经保存的自定义脚本不受影响。
- Canvas Agent Codex 日志：Codex app-server 输出带颜色或样式控制符及 UTC 时间的 stderr 时，本地日志和网页诊断日志应只显示一份 `YYYY-MM-DD HH:mm:ss` 本地时间和干净文本，不应出现重复 ISO 时间或 `[2m`、`[31m` 等 ANSI 转义内容。
- Agent HTTP 诊断日志：网页发送一条普通消息后，本地 Debug 日志不应重复输出 `/health`、`/canvas/state`、`/canvas/activate` 成功请求、流式增量或完整会话响应，只保留 HTTP 请求与 Codex 生命周期摘要；右侧「日志」应以单行时间线展示发送、开始、回复、工具、完成用量和错误，不再输出 userMessage started/completed、流式摘要、重复 threadId 或大段原始 JSON。
- Agent 排查日志：右侧「日志」中的排查视图应显示连接摘要，并可按错误、警告和信息筛选；向下查看日志时仅列表内部滚动，标题、连接状态、筛选数量和操作按钮保持固定；连续相同事件应折叠计数，单条日志可展开查看完整详情；原始 JSON、复制全部、复制最近错误和清空功能应继续可用，浅色与深色主题下均应清晰。
- Agent 排查日志顺序与跟随：日志应按时间从旧到新排列，进入日志或切换筛选时定位到最新事件；停留底部时自动跟随新日志，展开最后一条后应继续停留在完整详情底部，向上浏览后暂停跟随并显示与对话区视觉高度一致的居中圆形向下箭头，新日志数量只在悬浮提示中显示；列表底部不应出现额外留白，点击按钮后回到底部并恢复跟随。
- Agent 对话统计：用户消息应右对齐并使用透明无气泡的简洁排版，用户和 Codex 两侧均不显示人物头像，消息下方均不显示时间或 Token 信息；输入框上方应居中展示最新一次模型调用的输入、缓存、输出 Token 用量，不显示会话累计值，数值更新时应从旧值平滑滚动到新值而非突然跳变，新建、切换或删除当前会话后应清空旧统计。
- Agent 回复实时显示：在右侧 Agent 发送消息后，用户消息下方应立即出现“正在思考...”，任务运行期间不应闪退；工具完成后应显示 Codex 正在继续处理及已等待时长，等待超过 30 秒时提示可继续等待或停止本轮；Codex 的回复应在当前对话中持续显示，实时事件缺失时也应在任务完成后自动同步完整内容，无需切换到历史或日志再返回对话。模型繁忙等任务失败时应立即结束等待状态，在对话中显示中文错误原因和重试建议，诊断日志不应再把失败轮次记为“处理完成”，刷新历史后错误仍应保留。新建尚未发送首条消息的空会话不应反复出现历史读取失败。
- Agent 对话实时与历史一致性：运行包含思考、命令及画布工具的较长任务，在执行中刷新发起页面或打开第二个页面时，已出现的用户消息、思考过程、工具卡和回复前缀均应保留并继续更新；同一思考或工具只显示一张卡片，任务结束及再次刷新后内容、顺序和状态应一致。切换页面焦点不能改变本轮画布工具目标，发起页面断开后不得把图片或写操作转交其他画布，使用相同页面身份重连后才能继续处理。
- Agent 流式交互性能：发送长回复时文字应连续平滑出现，输入框、滚动和画布操作不应随回复变长而明显卡顿；长历史会话中只有当前流式消息持续更新，屏幕外消息不应造成明显布局压力；任务完成后应通过 SSE 自动同步完整历史，不再持续请求 `/health` 轮询状态。
- Agent 过程时间线：新建 Agent 对话后 Codex 应生成可读思考摘要，并在运行时依次显示中文的思考摘要、执行计划、命令执行、网页搜索、文件修改和画布工具活动；思考摘要的图标、标题和箭头应稳定保持在同一行，默认收起且无边框，点击箭头后才展开 Codex 实际返回的具体摘要，其中 Markdown 强调、列表和代码应正确渲染而非显示原始符号，多行代码块和文本流程图应保留原始换行并允许横向滚动，完成事件不应再用「已完成分析」覆盖已经收到的摘要；同一轮连续命令应合并为显示数量的无边框折叠行，折叠时不显示命令预览，单条展开后直接显示详情，多条展开后可逐条查看工作目录、耗时、退出状态和运行输出；其他工具调用继续使用紧凑卡片排版，状态图标、标题和状态文字保持在同一行；结构化任务进度不应混在对话时间线中，而应独立放在对话区下方、Token 统计上方，支持展开和折叠，并逐项实时更新「待处理」「进行中」「已完成」状态；新任务生成时默认展开，任务结束后保留最新结果，同一计划更新不应生成重复内容；文件详情展示文件路径与新增/修改/删除动作，工具详情不应出现请求 ID、英文工具名或原始 JSON；一轮对话结束自动同步历史以及刷新页面后从历史中 resume 对话时，思考摘要、命令和工具过程记录仍应完整保留并保持相同展示。
- Agent 权限控制：输入框可选择「请求批准」「自动审查」「完全访问」；请求批准模式下，Codex 编辑工作区外文件、执行受限命令或访问网络时应在对话中显示审批卡片，支持拒绝、允许一次和本会话允许，并能依次处理多个并发请求；提交决定后卡片应保持禁用等待，只有 Codex 确认处理后才移除；等待审批时刷新页面，未处理的审批卡应自动恢复且不能让任务永久卡住；自动审查模式应仅将需要用户决定的风险操作送入审批；完全访问必须先显示风险确认，启用后可访问网络和本机文件且不再请求审批；选择应在刷新后保留。
- Agent 历史记录：点击记录卡片应直接进入对应对话，不再显示「进入」按钮；可勾选单条或全选多条记录并批量删除，删除当前对话后聊天内容应清空。
- Agent 默认新对话：每次进入任一画布并连接 Agent 后，对话区应保持空白且不自动恢复上一次会话，并在后台创建新线程、预热 MCP；需要继续旧对话时可在「历史」中主动选择恢复。
- Agent 当前画布优先：在已打开某个画布时要求 Agent 创建、修改、整理或生成内容，Agent 应直接读取并操作当前画布，不应先调用 `canvas_list_projects` 或使用 `site_navigate` 重复进入画布；只有明确要求查看或切换其他画布时才允许查询画布列表并导航。
- Agent 图片消息：发送一张或多张图片附件后，图片应紧跟用户文字并在消息右侧显示为约 40px 的紧凑缩略图，不再撑大消息区域，单击缩略图应打开大图预览，打开或关闭预览不应改变消息间距或产生额外空行；任务运行中、完成同步历史、切换页面及重新进入历史对话后都应显示浏览器本地保存的图片缩略图，不应消失或把 attachmentId、附件使用说明等内部上下文回显到用户消息中；删除历史会话后应同步清理对应缩略图。
- 画布文本复制：在 Agent 对话或节点信息详情中用鼠标选中文字后，按 `Ctrl/Cmd + C` 应复制所选文本，不应触发画布节点复制；未选中文字且焦点位于画布时，原有节点复制快捷键应保持可用。
- Agent 工作目录指令：`canvas-agent/agent-instructions.md` 应作为独立维护源；重启 Canvas Agent 后，当前工作目录应自动生成 `AGENTS.md`；新建对话发送消息时，Codex 日志中的用户消息只包含本轮请求和必要的附件上下文，不再重复整段 Infinite Canvas 前置提示词，画布及工作台工具仍可正常调用。
- 画布文本设置：文本节点和生成配置节点切换到文本模式后应显示推理强度设置，可选择自动、低、中、高、极高；选择自动时默认 OpenAI Responses 请求不应携带 `reasoning`，选择其他档位时应携带所选强度，刷新画布后节点设置应保留；文本模型自定义调用脚本应能读取 `reasoningEffort`，OpenAI 模板应按自动或指定档位正确组装请求。
- 生图工作台参考图：将一张或多张图片拖入参考图区域后应直接上传并显示缩略图；拖入非图片文件应忽略，拖动过程中区域应显示高亮提示，放下文件不应导致浏览器打开或替换当前页面。
- 工作台历史图片清理：生图或视频工作台生成内容后，在「我的素材」删除图片、删除画布图片节点、清空画布或删除画布，再次打开对应生成记录时，历史缩略图、结果图片和参考图仍应正常显示；删除生成记录时，其专属本地文件仍应正常清理。
- 视频创作台参考资产：将图片、MP4/MOV 视频或 MP3/WAV 音频拖入任一参考资产区域后，应按文件类型自动上传到对应列表；当前拖入区域应显示高亮提示，原有数量、大小、时长和格式限制应继续生效。
- 画布组装提示词：输入或连接超长提示词后，浮层正文高度不应继续撑大，内容应可在正文区域内滚动查看和编辑，标题及关闭按钮保持可见。
- 画布节点提示词：图片等节点下方的提示词超过输入区域高度后，鼠标悬停在输入区域内滚动应查看提示词内容，不应缩放画布。
- 画布节点提示词回显：单图或多图生成完成后，切换选择生成结果的根节点及任一子节点，下方输入区域都应继续显示生成时的提示词；切换到其他节点再选回来后也不应清空。
- 画布生成配置：连接文本节点后连续点击生成（包含失败重试），组装提示词不应重复追加上游文本；在生图、文本、视频、音频之间切换时，实际请求应始终使用当前面板展示的对应类型模型。
- 画布多图生成：开始生成 N 张图片后应立即创建一个包含 N 个可展开槽位的图片节点，并且来源到结果只保留一条连线；生成期间空槽位应独立显示生成中或失败，任何已有图片内容的槽位都必须始终显示图片，任意图片率先成功后应立即成为主图并展示，不等待其他请求完成；收起状态应最多显示三层紧密错开的背板，避免占用过多画布空间，右上角应显示醒目的「N 张」数量提示；点击数量后，浮动工具条应暂时隐藏，各图片槽位应从当前节点向右上方整齐展开；已成功图片的「设为主图」应常驻显示，并在亮色、深色主题及不同图片背景上保持清晰；点击画布空白处应自动收起并恢复工具条。
- 画布多图任务关联异常：恢复批次时若某个图片槽位找不到对应服务端任务，应显示失败状态，不得持续显示加载中。
- 画布多图主图切换：点击「设为主图」后，节点中心和当前最长边应保持不变，仅按新图片比例调整另一条边；自由变形节点应完全保持现有宽高，画布视口及左下角缩放值不应变化。
- 画布多图失败槽位：包括当前主槽位在内的每个失败槽位都应显示「重试」和「删除」；重试只更新当前槽位，已有成功主图时不得抢占主图；删除只移除当前失败槽位并同步更新图片数量。
- 画布多图尺寸调整：子图展开时开始拖动尺寸控制点，应先收起图片组一次，再正常调整根节点尺寸，不应触发 React 循环更新或应用错误页。
- 画布左侧元素列表：点击元素整行应平滑定位并选中对应节点，自动缩放不得超过 100%；有内容的图片元素应显示预览按钮，点击后打开大图弹窗且不触发画布定位。
- 配置与用户偏好：导出 JSON 后应包含渠道、默认模型、生成偏好、提示词来源和 WebDAV 配置；在修改当前配置后重新导入该文件，应恢复导出时的设置，错误 JSON 文件应提示格式不正确。配置文件包含 API Key 和 WebDAV 凭据，不应公开分享。
- WebDAV 画布删除同步：将已同步到 WebDAV 的画布在本地删除后再次点「立即同步」，该画布不应从远端恢复到列表；刷新页面后再同步，已删除画布也不应回来。另一台设备新建且未删除的画布同步后仍应出现。我的资产和生成记录的删除同步不在本次范围。
- 本地存储设置：配置页应显示「本地存储」Tab，进入后只读取 `infinite-canvas` 数据库，自动统计 IndexedDB 占用、站点总占用、浏览器配额与使用率，并按对象仓库显示记录数及内容体积；新增或删除图片、音视频、生成记录后点击刷新，数据应同步变化，统计期间页面保持可操作。
- 生成提示词的文本编号：普通节点或生成配置连接多个文本节点后发起生成，附加在提示词末尾的每段上游文本都应以「【文本1】」「【文本2】」开头并按连接顺序编号，编号与提示词里 `@` 引用及参考内容栏显示的一致；单段文本同样要带编号，图片、视频、音频编号不受影响。
- 局部遮罩编辑：弹窗底部应为「重置」「导出到画布」「立刻生成」；两个按钮都要求已涂抹区域并填写修改要求，「导出到画布」只写入节点和连线不发起请求，结果节点的提示词面板应预填拼好的提示词并可手动生成，「立刻生成」在写入节点后立即生成。涂抹区域并填写修改要求后确认，画布应在原图下方生成一个「遮罩标注」图片节点，内容为原图叠加半透明蓝色标注区域，颜色和透明度应与弹窗里涂抹时看到的一致；结果节点在原图右侧生成，并同时接收来自原图和遮罩标注的两条连线。请求应按普通参考图发送两张图片，提示词按「图片1 / 图片2」编号说明原图和标注区域，不再携带 mask 参数，Gemini 格式渠道也应可用；生成结果只改动标注区域，且不得残留蓝色标注。
- 图片编辑弹窗：遮罩、切图和裁剪连续滚轮缩放时，图片与遮罩应保持同步且不再闪烁、短暂消失或跳动；遮罩画笔圆心应始终固定在鼠标位置，仅直径随缩放变化，缩放后仍可准确涂抹、拖动切分线和调整裁剪框。
- 提示词中心布局：页面标题及提示词总数应居中；连续输入搜索文字时应在停止输入约 300ms 后再查询；桌面端分类与标签应在左侧独立滚动，右侧搜索框下直接展示提示词卡片；标签数量较多时不能继续向下挤压提示词，窄屏下应恢复上下排列且内容不溢出；不再显示「我的提示词」Tab，收藏提示词应直接加入我的资产。
- 提示词详情弹窗：封面和参考图应固定显示在上方，复制及加入资产操作栏固定在底部，只有中间的标签、描述及提示词内容区域可以滚动；弹窗宽高应受视口限制且不超出屏幕。
- 提示词来源：7 个内置来源应从 Image Prompts 统一仓库读取，更新后数量依次为 323、494、517、53、76、126、129；提示词仍按 7 个来源分组并可独立启用，来源内可继续按 `tags` 筛选。添加标准 JSON URL 后应能查看内容，填写非数组 JSON 或不可访问地址时应显示失败，并继续保留该来源上一次成功缓存的内容。
- 提示词来源界面：来源应以卡片列表展示，启用开关位于左侧，数量、同步状态和上次成功时间作为次级信息显示，查看、拉取及自定义来源编辑/删除操作使用带文字按钮；底部定时拉取区域应保持独立边框布局。
- 画布提示词库：不应再显示「我的提示词」分组；不展开任何公共来源直接搜索其中的提示词，匹配项应自动显示；点击「插入画布」后应创建正文正确且标题保持为提示词标题的文本节点。
- 全站 Agent：新增 `generation_get_status` 工具，画布生成节点可按 `nodeIds` 查询，生图和视频工作台提交后会返回 `taskId` 并可查询排队、运行、成功或失败状态；需验证查询只由当前活动标签页返回。
- 本地 Agent 多标签页隔离：同时打开两个不同画布并连接同一个 Agent，分别聚焦标签页后通过 MCP 读取和修改画布，操作应只落在当前聚焦页面；网页面板发起的整个 Codex turn 应固定操作发起页面，即使中途聚焦另一标签页也不能切换目标；非运行状态关闭当前页面后应回退到最近聚焦且仍连接的页面，运行中关闭发起页面则不得把操作转交其他画布，只有同一页面身份重连后才能继续；其他页面回传同一请求结果应被拒绝。
- 本地 Agent 多标签页会话同步：所有标签页共享同一个站点级 Codex 活跃线程；任一页面发送消息、新建、恢复或删除会话后，其他页面应同步活跃线程和聊天记录；Agent 输出仅显示在事件所属线程，运行中不能新建、恢复、删除或再次发送任务。
- 本地 Agent 运行状态同步：在一个标签页运行较长 Codex 任务，等待某张工具卡显示「工具完成」后再打开或刷新第二个标签页；第二个标签页应立即显示 Codex 正在运行并禁用发送，整轮结束后两个标签页同时恢复；工具卡只显示「工具完成」，整轮结束由「本轮完成」表示。
- 本地 Agent 图片附件落画布：在右侧 Agent 上传参考图并要求基于商品信息创建生图流程，附件应创建为保持原比例的真实图片节点，分析提示词应创建为文本节点，二者都应连接到生成配置节点；刷新页面后参考图仍可显示并参与生成。任务中途切换到其他标签页时，附件只能写入发起任务的标签页；若发起标签页关闭，附件读取应失败且不能落入其他画布。
- Agent 对话滚动：从历史或日志切回对话、恢复其他会话时应自动定位到最新消息；手动向上浏览后应显示与日志完全相同尺寸、位置和样式的居中圆形向下箭头，新消息不强制打断阅读，对话底部不应出现额外留白，点击按钮后平滑回到底部并继续跟随新消息。
- Agent 消息区分：用户消息应在右侧使用透明无气泡排版，AI 回复应在左侧使用无头像的开放式 Markdown 排版；长文本、多张附件、错误消息及浅色/深色主题下均应清晰且不溢出。
- Agent 命令记录：命令执行中应显示运行状态；完成后，同一轮相邻命令应合并为「已执行 N 条命令」折叠行且默认不显示原始命令，单条展开后直接显示完整详情，多条展开后可分别查看命令、输出和退出状态；对话结束或刷新后分组与记录不应消失。
- 跨域提示：生图、视频或音频请求被浏览器跨域策略拦截时，应明确提示需要通过自己的服务转发请求，不再只显示笼统的网络错误。
- 资产图片下载：我的资产中仍可预览的图片，点击「下载图片」应能保存文件；标题含中文、空格或「【】」时应沿用原标题作为文件名，无需改名；从画布刚存入、尚未刷新页面的图片也应能下载。
- 画布视频任务恢复：开始 OpenAI 风格视频生成后，视频节点应保存远端任务 ID。刷新页面后，尚未完成的视频应自动继续查询状态；若查询已停止，可点击「获取任务状态」继续用该任务 ID 拉取，直到生成成功、任务失败或查询请求报错。任务失败后，「重试」应重新发起生成而不是继续查旧任务。
Canvas Agent 配置权限

## 图片分辨率尺寸表

选择分辨率和宽高比后，W/H 按下表填写，单位为像素。预设尺寸不受「16 倍数对齐」开关影响；该开关只控制手动输入尺寸时的对齐。最终生成尺寸以所选模型的实际输出为准。

| 宽高比 | 1K | 2K | 4K |
| --- | ---: | ---: | ---: |
| **1:1** | **1024 × 1024** | **2048 × 2048** | **2880 × 2880** |
| **2:3** | 1024 × 1536 | 1360 × 2048 | 2336 × 3520 |
| **3:2** | 1536 × 1024 | 2048 × 1360 | 3520 × 2336 |
| **4:3** | 1024 × 768 | 2048 × 1536 | 3312 × 2480 |
| **3:4** | 768 × 1024 | 1536 × 2048 | 2480 × 3312 |
| **16:9** | 1536 × 864 | 2048 × 1152 | **3840 × 2160** |
| **9:16** | 864 × 1536 | 1152 × 2048 | **2160 × 3840** |
| **21:9** | 2016 × 864 | 2688 × 1152 | **3840 × 1648** |
| **9:21** | 864 × 2016 | 1152 × 2688 | **1648 × 3840** |

待验收：

- 在画布和图像工作台逐项切换 3 档分辨率与 9 种比例，W/H 应与表格一致，重新打开设置后仍选中正确档位；重点检查 1K 的 2:3、16:9、21:9 及对应竖图，以及 4K 的 1:1。
- 所有 4K 预设提交时均不应触发本地总像素或最长边限制；生成和编辑请求、调用脚本应收到所选像素尺寸。
- 分辨率为自动时保留比例选择，比例为自动时禁用 W/H；手动输入 W/H 后，对齐开关仍应按原有方式工作。
- Gemini 在质量为自动时，应将表中尺寸识别为对应的 1K / 2K / 4K；显式选择质量时维持原有优先级。视频尺寸设置应保持原有行为。
