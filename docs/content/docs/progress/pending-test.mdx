---
title: Pending Tests
description: Implemented changes that still need manual verification
---

# Pending Tests

## Go 公益平台

当前分支已切换到 `server/` Go 服务。以下事项对应本次实际变更；下方公司版记录不代表 Go 平台已经通过同样的人工验收。

### 已有渠道升级启动修复待验收

- 已复现版本 13 存在渠道时新增非空 `capability` 列触发 `SQLSTATE 23502`，导致 API 退出的问题。版本 14 迁移现改为先填充类型，再收紧非空约束。
- 根据现有模型绑定确定类型；混用多种类型的渠道拆成独立渠道，保留原渠道与绑定 ID、加密密钥及启停状态、路由权重、成本和检测配置。每个拆分渠道继承原超时、并发和冷却设置，生成检测只保留归属自己的绑定；历史记录的原渠道引用继续保留。
- 无绑定的渠道按 Claude 协议或视频适配器确定类型，其余先归图片，可在后台编辑中更正。已有模型绑定时拒绝变更类型；类型修改与单个、批量绑定通过事务行锁互斥，避免并发写出跨类型绑定。
- 后端完整 54 组测试（含子用例共 87 项）与前端 53 项回归全部通过，无失败或跳过。已覆盖单类型、混合类型、无绑定、已删除渠道、长渠道名、密钥状态、绑定及成本保留和未绑定渠道类型更正。
- 修复版前后端 Docker 镜像构建通过；含旧渠道、密钥和四类模型绑定的版本 13 数据库已成功升级到 14，API 健康，绑定元数据摘要一致。再次启动没有重复拆分渠道或复制密钥。记录位于本机忽略目录 `data/local-verification/channel-startup-1789259365549/`。
- 服务器需拉取本次修复并重新构建启动，确认 API 恢复、各类渠道及模型绑定正常；升级前备份 PostgreSQL 与平台加密密钥配置。

### 模型与渠道分类型配置待验收

本机前端与 Go API Docker 镜像已构建并启动，TypeScript 检查通过，入口为 `http://localhost:3301`。独立 Docker 数据库、Redis、MinIO 下，后端完整 52 组测试（含子用例共 85 项）及前端 53 项回归全部通过，无失败或跳过；后端覆盖四类渠道保存、16 种类型组合的单个与批量绑定、非法配置和类型修改拒绝、分类检测，以及错误绑定下的调度、报价与在线调试拦截。

同一前后端镜像的隔离浏览器验收已验证四类模型保存、文本计费重新编辑与缓存零价、图片 `0.008` 单张价格、视频及音频秒价、分类搜索、刷新保留类型、深浅主题、渠道协议筛选与 Gemini/Veo 适配器保存；未发现浏览器脚本错误。验收数据均在独立测试数据库中，未调用真实上游。

升级前已备份并校验 PostgreSQL、MinIO、Redis 和本机环境配置；主站数据库已从版本 13 升至 14，账号、钱包、账本、签到、加密 Key、模型配置与运营设置摘要一致。服务健康检查、登录页及两类管理路由返回 200，记录位于本机忽略目录 `data/local-verification/channel-types-1789227478684/`。

- 模型管理和渠道管理均提供图片、文本、视频、音频四个标签页，分别显示数量、搜索结果与空状态；刷新或在两个页面之间跳转后保持当前类型，新建记录归入当前类型。
- 模型编辑与渠道快捷建模使用相同的分类表单：图片按张计价；文本按次或 token 计价，并配置最大输出 token 与思考强度；视频、音频按次或按秒计价。核对零价、缓存免费、切换计费模式、保存后重新编辑，确认不同类型的字段不会串入请求。
- 每个渠道对应一种类型，接口、密钥与调度参数独立保存；未绑定模型前可以更正类型。文本渠道支持 OpenAI、Gemini、Claude Messages，其余类型提供 OpenAI 与 Gemini。视频适配器仅在视频渠道显示，选项随协议变化；超时 300 秒、并发 20、故障冷却 120 秒等原有默认值保持不变。
- 模型绑定、渠道快捷建模与批量绑定只选择同类型记录，唯一同名模型也在同类型内匹配；后端拒绝跨类型绑定、修改已有模型绑定渠道的类型、非文本 Claude 渠道及非视频任务适配器。报价、调度与在线调试同样核对渠道类型。
- 「检测图片/文本/视频/音频渠道」仅提交当前类型内已启用且配置了检测项目的渠道；核对其他类型不被排入检测。在线调试切换图片/文本时清空上一次的渠道选择。
- 数据库版本 14 为渠道增加必填 `capability`，现已支持已有渠道回填和按类型拆分；初版仅验证空库的不足已由上方启动修复覆盖。前后端需一同更新。
- 窄屏交互、真实渠道的快捷建模、生成计费及生产 HTTPS 反代仍待人工验收；本机隔离验证不代表实际供应商与生产环境已验证。

### 本机 Docker 验证结果

以下为分类型配置改动前的验证基线，不代表本轮页面已经完成浏览器验收。

前端与 Go API 镜像均已构建并在 `http://localhost:3301` 启动，TypeScript 检查通过。独立 Docker 数据库、Redis、MinIO 下，后端 50 组测试（含子用例共 67 项）全部通过；前端完整 53 项回归通过，均无失败或跳过。

7 组经 Web Nginx 的隔离接口场景通过，覆盖登录与权限、管理员模型配置、价格精度、思考档位校验、输出上限、多轮上下文与新对话隔离、重复请求、三种文本协议的真实 Worker 调度与结算，以及注册推荐关系。支付回调验签、重复返利、比例变更和事务回滚由后端集成测试覆盖。

浏览器已验证登录后状态页、`0.008` 模型价格、六档思考选项、切换模型自动回到可用档位、隐藏用户输出上限、后台保存后前台立即更新、刷新续聊、新建对话清空设置、深浅主题及邀请页百分比；未发现浏览器脚本错误。移动端细节、真实上游与商户支付、生产 HTTPS 反代仍待验收。

替换服务前已备份 PostgreSQL、MinIO、Redis 和 `.env.local` 并校验备份；启动后余额、账本、签到、加密 Key、模型配置和运营设置摘要一致，数据库仍为版本 13。运行日志、接口结果与备份位于本机忽略目录 `data/local-verification/20260912-164419/`。

### 模型思考强度与当前对话上下文待验收

开发阶段的配置校验、多协议和前端回归已纳入上方本机 Docker 完整验证；实际渠道支持的档位与生产环境仍需确认。

- 管理员在模型编辑和渠道快捷建模中勾选 `low`、`medium`、`high`、`xhigh`、`max`、`ultra`，只为实际支持的模型/渠道开放对应档位。保存、编辑、清空配置后，文本对话、工作台及画布同步展示；未配置时仅使用「自动」，不发送显式思考参数。
- 切换模型或打开历史对话时，已选档位不再可用则显示并提交「自动」；后台拒绝未开放的档位、冲突的参数别名与重复/非法配置，拒绝发生在冻结前。保存模型后重新进入文本页面应立即读取新选项。
- 协议核对：OpenAI 兼容接口使用 `reasoning_effort`，Claude 使用 `output_config.effort`，Gemini 使用 `generationConfig.thinkingConfig.thinkingLevel`；Gemini 原生目前只选择支持该字段的模型与 `low/medium/high` 档位。核对自动模式不添加思考参数，输出上限仍取管理员设置。
- 当前窗口上下文：在同一对话中先提供一个代号，再追问代号；刷新页面、切换模型后继续同一对话，应仍能引用历史。新建对话后不应知道该代号，文本页面系统提示词与思考设置同时重置；重新打开原历史对话仍能续聊。
- 上下文按 `conversationId` 从服务器读取，保持账号隔离，不引入跨对话用户画像或长期记忆；当前继续使用原有历史消息，不新增摘要策略或截断阈值。

### 邀请返利与文本交互调整待验收

支付返利、价格精度及管理员输出上限已纳入上方本机 Docker 完整验证；真实支付商户联调仍待完成。

- 邀请充值返利：在「平台运营设置」开启返利并填写百分比，例如 10 表示 10%。好友注册只建立关系，真实支付的每笔充值按到账时配置返给邀请人；注册、签到和兑换码不触发返利。默认关闭、比例 0，旧的固定注册奖励字段不再使用。
- 返利账务：核对小数比例、精确到分的充值金额与微元精度，不足一个微元的部分舍去。重复回调/查单只返利一次；比例变化与重新开启不会补发已入账订单，返利失败时充值、订单状态及账本一起回滚。邀请页、后台记录与钱包明细显示累计返利，已到账余额永久保留。
- 模型价格：管理员保存 `0.008` 后，后台模型列表、图片工作台、创作工作台、画布模型选择与文本模型菜单均显示 `0.008`，不补成 `0.008000`；核对免费价格、缓存单价、每秒计价及分组折扣后的实际价格。
- 登录后的模型状态：桌面顶部「模型状态」、移动导航和个人中心均能进入 `/status`，页面保留工作台导航；文本对话侧栏不再承载状态入口。未登录跳转登录、状态接口返回 401，切换账号后不复用上个账号的查询缓存。
- 文本输入区：模型与思考强度放在输入框底部、发送按钮旁，模型菜单可搜索并展示价格。核对管理员配置的可用档位、历史恢复、发送与取消、窄屏和深浅主题；具体思考强度规则见上方本轮验收项。
- 管理员输出上限：在模型编辑或渠道快捷建模时配置正整数 `maxOutputTokens`，无默认值；用户文本对话、创作工作台和画布均不再出现 token 输入项。未配置时在冻结前拒绝，用户伪造各类 token 参数不能覆盖后台设置；报价、任务快照和上游请求使用同一上限，重复请求保持原快照。

### 渠道调度与开放能力待验收

- 模型状态登录可见：未登录访问 `/status` 会跳转登录页，直接调用 `/api/status/models` 返回 401，登录后状态页正常展示；登录页不再出现「查看模型运行状态」入口与「签到领取创作余额，选择模型即可开始」文案。
- 登录页：左下角不再显示「邀请加入 · 每日签到 · 按需充值」；登录表单下方为「注册」与「忘记密码」同一行链接，点击「注册」切换注册表单后显示「已有账号，返回登录」，两步验证状态下保留「返回账号密码登录」。

此前渠道扩展已完成本机 Docker 构建、启动与回归：后端 44 组测试（共 55 个含子用例的通过记录）及前端 52 项全部通过，0 失败、0 跳过。已补充 `server/internal/platform/routing_moderation_test.go`，修正登录测试沙箱缺失 `URLSearchParams`、多模型探针依赖外部 DNS 的测试夹具；生产超时与频控默认值未因测试调整。这些结果不覆盖上方本轮调整。

已有本机数据库从版本 11 升至 13，升级前备份 PostgreSQL、MinIO、Redis 和 `.env.local`；钱包、账本、签到及加密 Key 的数量与内容摘要前后相同。`http://localhost:3301` 的 API、Web 与三项存储依赖运行正常，健康、登录、文本、状态、素材页及 JS 入口均返回 200。

独立数据库与模拟上游下的 7 组接口验收全部通过，覆盖 Nginx 登录、单次冻结与故障转移、Key 去重/停用、审核通过与拒绝退款、Claude 原生协议及用量、检测停用恢复、匿名状态、邀请/签到奖励和图片存储；公开状态页浏览器渲染正常，无脚本错误。真实上游、商户支付、SMTP、生产 HTTPS 反代、备份恢复演练及持续压测仍需验收。

- 永久余额：分组页面不再出现周期赠送、周期消费上限，周期领取接口应返回 404；升级后原余额、冻结额、签到与历史账本不变，兑换码继续可用。本月统计不改变余额。
- 自动切换：默认每任务最多 3 次（含首次），按优先级和权重选候选；限流、超时和上游故障切换，鉴权失败停用对应 Key。核对不同实例继续同一任务时只冻结一次、成功只结算一次；耗尽或没有剩余候选后全额退回冻结。
- 不重复提交：内容政策、无效参数直接失败；流式已输出文本、已取得视频任务 ID、或上游结果已成功返回后，不再次发起生成。服务重启与迟到响应不得突破尝试次数、覆盖取消结果或重放已知拒绝。
- 多 Key：验证追加去重、轮询/随机、单独启停与删除；一个 Key 鉴权失败不能停用其他 Key 或整条渠道。已受理视频在更换 Key 后仍按原快照续查，管理接口不返回明文。
- 健康处置：连续 3 次有效检测失败停止接单，冷却后按原配置检查，1 次成功恢复；手动停用不自动开启。无效参数、内容拒绝和本地存储错误不累计为健康故障。未开启自动检测时需手动触发恢复检查。
- 耗时与公开状态：任务/探针/调试成功后，渠道列表 p50/p95 与保存样本一致；登录后访问 `/status` 和 `/api/status/models`（未登录返回 401 或跳转登录），未检测及自动检测过期显示待检测，部分渠道可用时显示部分可用；响应不包含密钥、地址或检测提示词。
- Claude Messages：核对原生请求头、系统提示词、图片附件、SSE 完成标记及输入/缓存/输出用量。用户生成采用管理员配置的最大输出 token，达到上限正常结算；管理员的渠道检测和调试台仍可填写独立检测参数。
- 人工审核：`review` 进入待审核，`log` 放行仅记规则，`block` 优先拒绝；文本及图片/视频/音频提示词、文本参数均检测。验证普通用户不能审批，通过后入队，拒绝/取消退款幂等，批量请求和重新手动重试不绕过审核；被拒绝的文本不会成为下一轮上游上下文。
- 邀请返利：推荐链接不能替代注册邀请码；注册返固定奖励已替换为上方的充值比例返利，需按新规则重新验收。
- 用户文本对话：`/text` 支持报价、系统提示词、历史续聊、流式回复和服务端取消；刷新恢复历史与思考强度，最终消息只出现一次；换账号关闭旧订阅，不显示旧会话或写回旧请求结果。
- 任务适配器：现有 OpenAI 兼容视频、Gemini/Veo 经统一适配器提交与续查；核对时长与价格快照不变。即梦、可灵、Vidu、原生 Sora、Suno 只有待接入入口，收到实际 API 文档后才能接入验收。

### Earlier code review fixes awaiting acceptance

- Startup migrations now include channel soft deletion, the distinct uploading state, and probe deadlines (`009`–`011`). Verify both an empty database and an existing Go database, including channel/model access after startup.
- Cost totals must show positive user payments: a known upstream cost of ¥1 and payment of ¥0.6 should produce a ¥0.4 subsidy.
- Canceling an upload or failing to mark it ready must release its quota using a live cleanup context. If object deletion fails, retain a deleting record for the existing cleanup job; active uploads must remain reserved and untouched by that job.
- Active monitoring now selects and stores `bindingIds`. Re-select and save existing monitoring configurations. Verify two upstream bindings of the same platform model are probed independently with their own cost settings, and foreign-channel bindings are rejected.
- Playground requests share channel slots and wait through congestion or cooldown; execution uses the configured channel timeout without the former 60-second cap. Canceling or leaving the page stops the request without replaying it. Completed, failed, and interrupted calls must retain separate cost records without changing the user's balance; ambiguous/unconfigured costs remain unknown. After a restart, unfinished probe records expire at their original deadline.
- Playground results must show the actual HTTP status and native response JSON or SSE event payloads; text includes first visible text latency and images include a preview. Channel credentials must be redacted, and large raw responses should be formatted only when expanded or copied.
- Asset creation, editing, deletion, and import must wait for server success; failures retain inputs and show an error. Prompt-library and video saves, plus canvas-side deletion, follow the same rule. ZIP imports keep text, image, video, and audio assets as private copies and report the actual successful count.
- Images inserted from the asset library must be included in exported canvas ZIPs and survive import into another account. Account changes during file preparation, saving, or export must discard stale callbacks.
- Recovery-code login must persist a specific MFA-disabled/rebind notice and queue email when enabled, in the same transaction as consuming the code and revoking sessions.

Local Docker validation passed: both production images build successfully, all 36 backend test groups (39 cases including subtests) and 52 frontend regression tests pass with no skips. The existing local database upgraded from version 8 to 11 while preserving its volumes and credentials; PostgreSQL, MinIO, Redis, and local configuration were backed up first.

Seven API scenarios passed against isolated dependencies and mock upstreams, covering authentication/password change, multiple bindings, text/image diagnostics, native responses and redaction, queue cancellation, media/asset operations, and cost accounting. Browser checks confirmed text output, the HTTP 201 image preview, and redaction; asset inputs survived a simulated API outage, then saved successfully and remained after reload.

Validation also fixed an unsupported native-button `loading` prop, missing test helpers, the concurrency fixture, and 204 assertions for empty responses. Nginx no longer treats the `/assets` page as a static directory or redirects it to internal port 3000; direct page access, reload, and static bundles work. Real upstreams, merchant payments, SMTP, production HTTPS proxies, backup restoration, and sustained load remain pending.

### Backend capabilities implemented in this iteration

- Frontend page layout and column allocation optimizations:
  - Redemption page: converted to a full-width card layout with 4 top summary metric cards (total, active, used/disabled, total loaded amount); column widths proportionally rebalanced (code hint 220px with one-click prefix copy, amount right-aligned, usage progress bar, centered status tags, expiration & creation timestamps, ellipsis notes); expanded batch result modal to 720px with 2-column responsive card chips.
  - Usage analytics dashboard: structured metric cards into a balanced 12-slot 2-row grid without orphaned cards; optimized the channel attempts table with proportional column widths, adaptive whitespace, colorful micro-progress bars for success rates, and CSV export.
  - Channel playground: upgraded to fill full viewport height on desktop without outer scrollbars, aligning left and right panels with independent internal scrolling and clean responsive layout.
  - Notification center: expanded Popover width to 380px ~ 440px and list max-height to 380px with subtle rounded item hover states.
- Lightweight notification system: removed the 2.5s polling storm from `NotificationCenter`, replaced the drawer with a lightweight Popover adhering to canvas themes, integrated system notices and announcements in dual tabs, and introduced `POST /user/notifications/read-all` for one-click read.
- Multi-dimensional analytics dashboard: expanded `GET /api/admin/stats` with `byDates` time-series aggregation and `byCapabilities` breakdown; added daily request & cost trend bars and capability distribution ratios to the admin stats dashboard.
- Batch redemption code generator & exporter: supported `count` parameter in `POST /api/admin/redeem-codes` for batch generation (up to 100 codes); added batch result modal, one-click copy all, and TXT/CSV file downloads in the admin UI.
- Online channel playground: added `POST /api/admin/playground/test` admin debugging endpoint and an interactive `/admin/playground` page to test text/image upstream routing, measuring latencies, first token delay, HTTP status, and raw response without user balance deduction.
- 后端审查修复（待人工验证）：
  - 空结果扣费：图片/视频/音频任务上游返回 HTTP 200 但结果为空时，按上游失败处理并退回冻结余额，不再出现“已扣费但无输出文件”；结算前有最终防护。
  - 视频时长一致性：提交 `seconds` 与 `durationSeconds` 两个不同值会被拒绝（400 `invalid_duration`）；创建任务时统一规范化时长，Gemini 视频上游请求、冻结额、完成计费使用同一时长。
  - 存储占位回收：MinIO 上传/生成结果落盘明确失败时立即删除占位记录，页面用量与配额判断不再被失败上传长期占用；结果保存失败保留 `storage_quota` / `timeout` 错误分类，用户能看到“删除文件或联系管理员”的可操作提示。
  - 订单对账公平性：到期待确认订单每轮随机选取 100 笔，最早一批持续查单失败的订单不会再阻塞后续订单自动确认。
  - 成本统计口径：管理端成本页的“实付/补贴”改为与成本相同的模型/渠道/来源筛选范围归集，并按任务去重，筛选单个模型不再被其他模型收入抵消差额；全平台口径与之前一致。
  - 检测中断恢复：多模型渠道检测的全部成本记录通过新增的 `monitor_token` 列关联同一次检测执行，进程中断后一并标记失败，不再遗留 `running` 记录；新增迁移 `007_monitor_cost_link.sql` 添加该列，启动时自动升级。
  - 代码复用：管理端“获取模型”“查询余额”复用主动检测的统一实现，行为与主动检测一致（去重、无额度时报错）。
  - 测试编译：修复 `capabilities_test.go` 对 map 元素取地址导致的整包测试无法编译。

- Upstream error propagation and safety policy handling: detailed upstream error messages (e.g. `status_code=400, 您的请求无法用于生成图像。该请求可能因安全政策被拦截，或不适合进行图像生成。`) are extracted, correctly categorized as non-retryable content policy errors, persisted to task error messages, and displayed directly to users on both canvas and workbench instead of being masked by generic static strings.
- Channel creation default parameters: the admin create-channel modal defaults to 300s timeout (300,000ms) and concurrency 20, matching the database schema defaults.
- Channel soft deletion and model unbinding: deleting a channel that already has tasks or logs marks it with `deleted_at = now()` and unbinds associated `model_channels` instead of failing with a foreign key constraint violation. All operational queries exclude soft-deleted channels.
- Model pricing display on canvas and workbench: `ModelPicker` displays price badges in dropdown items and trigger summary; `use-config-store` preserves `pricePerImage` and matches models by both `name` and `displayName`; image workbench model select appends unit price `(¥0.008/张)`.
- Multi-upstream model binding decoupling and batch binding: multiple upstream models from the same channel can be bound to a single platform model without overwriting; batch binding modal supported in channels drawer; independent priority, weight, enabled status, and cost configurations per binding row in models drawer; scheduler eligibility check targets specific binding ID.
- GPT Image 2.5 adaptation: supports `gpt-image-2.5-flare` and `gpt-image-2.5-sunburst`; image settings panel supports `xhigh` (Ultra High) and `max` (Maximum) quality modes; requests omit `response_format` and pass `quality` to upstream properly.
- Billing: settle the hold and any additional charge once, preserve zero cache prices, distinguish calculated fees from actual charges, save per-second prices, handle string/numeric duration consistently, preserve group pricing on retries, and allow different redeem codes for one account.
- Channel operations: active generation probes, model changes and balance alerts. Automatic checks default off, share channel slots and cooldown, and recover interrupted checks without replaying the original generation. Verify state-change notifications and mail failure records with real channels and SMTP.
- Account security: verified email, single-use 30-minute recovery links, TOTP login, one-time recovery code and device revocation. Invalid/expired/replayed proofs must fail; password recovery must retain MFA, and revoked devices must lose access.
- Text streaming: OpenAI/Gemini partial text is persisted before one authoritative final message and settlement. Verify Studio/canvas progress, refresh/reconnect, local stop versus server cancellation, and account changes without stale text entering the next session.
- Group permissions and quotes: enforce allowed models on listing, quoting, creation, retry and execution; verify service quotes and actual charges. Periodic grants and spending caps have been removed; credited balances remain.

- 公网配置文件：服务器使用 `/opt/infinite-canvas-public/.env`，其中包含独立数据库、Redis、MinIO、对象桶、加密密钥和 `infinite_canvas_public_session`；该文件不得提交 Git。
- User notices: password, email, MFA and new-device login, recharge/grant/check-in credits, and daily low-balance alerts appear in the notification center; verified email plus SMTP also send mail. Same-device logins while a matching session remains do not repeat.
- Channel operations extras: a channel can probe multiple bound models, administrators can queue every configured channel, and models with no usable channel notify administrators at most once per hour.
- Costs UI: filter by date, source, model and channel, export the current page, and jump to unknown costs. Cleanup keeps cost ledgers and cost-reconciliation audits; channel checks (30 days), notifications (90 days) and other audits (365 days) are removed.
- Wallet and status: the wallet shows this month’s spend/credits, optional period cap usage, ledger filters and CSV export; `/user/stats` shows the same monthly totals; `/admin/status` shows queue, mail backlog, channel cooldown and storage dependencies.
- Costs: configure independent RMB costs on model-channel bindings, distinguish unknown/configured/reconciled costs, retain costs after request-log cleanup and failed result persistence, and inspect subsidy totals. Audio time-based pricing requires ffprobe, included in the Go Dockerfile.
- Sensitive rules: “record only” allows generation and records the matching rule without the full prompt; “block” still rejects. Inspect the recent-match table and audit.
- Verification: previous 30 Go test groups and 52 frontend regressions remain; local `.env.local` Docker frontend/API images have now been built and started, with API, PostgreSQL, Redis, MinIO health checks and `/health`, home, and login routes returning normally. Production `/opt/infinite-canvas-public/.env`, public HTTPS proxying, real SMTP/provider/payment integration, object-storage backup recovery, and target-hardware load testing remain pending.

### Earlier Go platform baseline

- 独立 PostgreSQL、Redis、MinIO 上累计 14 项后端测试通过：精确金额、并发充值回调、随机签到、邀请码名额、整批余额回滚、跨实例渠道槽位、取消与迟到结果、文本历史、视频原任务续查、媒体顺序/权限、SSRF、登录频控退款、管理员密码重置和公告版本。
- 浏览器已查看登录、四类创作工作台、钱包、支付配置和运营设置页面；继续验证邀请注册、昵称与账号安全流程、不同窗口登录退出、浅深色主题与手机布局。
- 已在隔离环境（模拟上游渠道）跑通端到端闭环：四类生成任务提交、冻结、结算、取消退款均正确（测试余额 ¥10 → ¥9.60），图片/视频/音频可保存为素材，插入画布后刷新可恢复三种媒体节点。
- 创作工作台图片页与视频页的尺寸记忆相互独立，直接打开视频页不再带入图片尺寸；批次任务全部落定后自动刷新钱包与账本缓存，冻结余额变化即时反映。
- New operations capabilities pending verification: user group discounts, redeem codes (admin creates, wallet page redeems, once per user per code), per-second video/audio pricing, channel balance query (channels page button via dashboard/billing endpoints; unsupported channels error out), and sensitive-word blocking (substring match on generation and text submission; review now queues manual moderation and log records without blocking). Database migrations support in-place v2→v3 upgrade; back up before upgrading.
- 以管理员配置图片、视频、文本、音频的真实 OpenAI/Gemini 兼容渠道，验证参数、参考资源、模型按结果收费、生成失败退款、后台停用渠道和任务取消。
- 文本模型 token 级计价：管理员给文本模型配置输入/缓存命中/输出每百万 token 单价后，提交时按历史长度与 max_tokens 预估冻结，完成时按上游 usage（OpenAI prompt_tokens_details.cached_tokens、Gemini cachedContentTokenCount）实际用量结算并多退少补；上游缺 usage 时回退按冻结额结算。已用隔离测试覆盖缓存计价与冻结清零。
- 验证画布刷新后图片/文本恢复及视频/音频续查，媒体下载、保存到素材与导入导出；删除生成历史后其他画布或素材引用应继续有效。
- 配置随机签到区间，测试北京时间跨日、同日重复点击和多个窗口并发领取；使用独立测试账户检查所有余额变动与账本对应。
- 完成易支付经典接口、支付宝官方当面付、微信 Native API v3 的真实商户联调：金额/商户不匹配拒绝、重复通知幂等、未支付查单关闭、迟到支付入账、密钥更换后旧单处理。
- 在目标服务器验证 1Panel -> Web Nginx -> Go API 的 HTTPS、Cookie、代理 IP、支付通知和静态资源路径；测试多实例扩容、Redis/数据库/MinIO 故障、备份恢复及持续压力下的延迟和成功率。
- 前端生产构建、Docker 镜像构建与真实支付未执行，生产吞吐量尚未验收。配置和启动步骤见仓库 `docs/public-platform.md`。

## 公司版基线记录

- Failover review: 30 isolated database and mocked-upstream cases pass. Recheck channel and model-binding eligibility after slot waits, publish cooldown before releasing capacity, and preserve failures newer than an in-flight success. Explicit refusals, including HTTP-200 refusal fields, must stop fallback without cooling a healthy channel; ordinary safety-service 503 errors must fall back. Health/attempt bookkeeping failures must not add model calls, and storage/result-persistence failures must not record successful charges. Existing timeouts, cooldown durations, concurrency limits, and the single extra fallback for ambiguous failures remain unchanged; real provider validation is still pending.
- Authenticated page review: canvases, version history, image workbench, assets, prompts, all four user-center pages, all six admin pages, and their main read APIs have been checked. The rebuilt local image has been verified to show only image/text generation choices, localized logout labels, and `0 B` for zero storage. The local setup has no published text model or connected Agent, so actual AI generation and Agent tool calls still need separate validation; two third-party prompt preview images are blocked externally.
- Public HTTPS security review: with 1Panel -> Web Nginx -> API and source ports closed, production cookies must include Secure; forged forwarding prefixes must not bypass login throttling, and untrusted browser origins must be rejected for mutations. Existing explicit `COOKIE_SECURE=false` must be updated in the deployment environment.
- Session isolation: switch from account A to B while project, asset, model, upload, or generation requests are pending. No A data, prompts, rollback results, cached queries, or Agent events should appear in B. Clipboard reads, canvas archive imports, reference-image reads, crop/split/upscale operations, and video-frame capture must not upload or submit old content under the new session. A stale upload's 401 must not log B out; other tabs sharing the cookie must invalidate their private state.
- Text recovery: first-time, multi-text, and retried generation must retain each slot's request and conversation IDs. A lost response after submission must recover the server's success or failure while preserving successful siblings; late responses must not overwrite recovered results. Failures before submission remain terminal; refreshing during retry preparation must not treat old failed requests as pending work. Concurrent retries of the same failed request, including UUID case variants, should call upstream once; completed replays retain the full response even after channel availability changes. Follow-up requests retain historical images in order, and system prompts use the system role.
- Canvas history media: migration `0010_canvas_history_media` retains existing server snapshot references. Removing an image from the current canvas, withdrawing its public asset, restoring the oldest retained snapshot, trimming history, and deleting the project must preserve and release exactly the right files. Failed or partially failed MinIO cleanup must retain metadata for a later retry.
- UUID concurrency: concurrent saves using different letter casing for the same canvas UUID must leave media references consistent with the final snapshot and retained history. Deleting an image batch with an uppercase UUID while a failed task is retried must reject the deletion and preserve the queued task.
- Upstream error redaction: Unicode escapes, escaped slashes, or long JSON error bodies reflecting channel credentials must not expose the decoded full key in user errors or request logs.
- Canvas history actions must wait for queued saves before creating/restoring a snapshot; a failed save stops the action. Foreground image waiting may finish while backend tasks continue, and those slots should later recover their authoritative server results.
- Optional local Codex integration: Agent bootstrap fragments must survive login and forced password change, and returning users retain their own local connection. Relogin during attachment, canvas-reference, or preview preparation must stop obsolete messages and attachments from being sent or restored. Adding an asset waits for server success, and workbench image commands retain the requested model and parameters after preference hydration. Platform text models remain separate; video/audio tools must not start unsupported server requests.

The current release needs manual verification in these areas:

- Upstream v0.18.0 sync & alignment: multi-selection box interaction with one-click group / ungroup; video generation task polling resumption with `videoTaskId` upon canvas reload; image generation dimensions aligned with aspect ratios and 1K/2K/4K presets with Gemini `imageConfig` protocol; multi-image reference protocol and local proxy forwarding integration.
- Channel failure cooldown configuration and self-healing: administrators can configure `cooldownSeconds` (0 to 86400 seconds, default 120s) per channel in `/admin/channels`; scheduler dynamically respects this cooldown period upon timeouts, 5xx, or rate limits; 401/403 errors cool down for at least 5 minutes or configured cooldown and automatically recover to active routing once the cooldown expires, avoiding permanent channel abandonment.
- Text retry idempotency and media reference safety: retrying a failed text request with the same `requestId` reuses the original conversation and user message, avoiding duplicate user messages in conversation history and preventing duplicate increment of attachment `referenceCount`.
- Image worker memory leak prevention and attempt consistency: reference images are guaranteed to be released in a unified `try...finally` block even if pre-generation DB queries fail; `generationAttempts` are only marked `succeeded` in the same database transaction as the successful task completion, preventing orphan succeeded attempt records on storage upload failures.
- GPT-image model compatibility: requests for `gpt-image-*` models omit `response_format` in both generation and edit payloads, avoiding upstream validation errors, while `dall-e-*` continues sending `response_format: "b64_json"`.
- Login rate limiter LRU eviction: `SimpleRateLimiter` refreshes key insertion order upon updating failure count, ensuring active attacked IPs are not evicted prematurely while dormant keys are purged when reaching capacity.
- Request log persistence order and billing consistency: image generation logs are marked succeeded and billed only after MinIO upload and Postgres persistence transaction commit; if MinIO or DB transaction fails, the task is marked failed, and the request log is updated to failed with no billed amount. Text generation request logs are likewise marked succeeded only after message persistence transaction commits, and marked failed on transaction errors.
- Reverse-proxy client IP and login rate limiting: both Compose files now pass `TRUST_PROXY` (default `true`) to the API container so the API resolves the real client IP from `X-Forwarded-For` behind Nginx; five consecutive wrong passwords for one account must trigger the 1-minute per-(IP + username) limit, and 30 cumulative failures from one IP across many usernames must trigger the 5-minute per-IP limit, both returning 429 with `Retry-After`. If the API port is ever exposed directly to an untrusted network, set `TRUST_PROXY=false`, otherwise clients can spoof `X-Forwarded-For` and bypass per-IP limiting.
- Text request retry after failure: resubmitting the same `requestId` after a failure must reuse that request record in place (status back to running, error code and response link cleared) instead of returning a "data already exists" conflict.
- Local Docker build and startup verified: both images build, all four services run, and API, PostgreSQL, and MinIO health checks pass. Web `/health` and the login page return 200 with no browser runtime errors; all 11 migrations through `0010_canvas_history_media` are applied. Two-hop proxy trust now uses Fastify's supported callback form, with existing proxy and login-throttling regressions passing. Authenticated pages and their main read APIs have now been checked using the existing account's signed-in session.

- Gemini header auth & NewAPI compatibility: Gemini upstream calls and channel probe tests pass `x-goog-api-key` in HTTP headers (and include `Authorization: Bearer` if the token starts with `sk-`), removing API keys from URL query strings to avoid leaking into logs; Gemini `baseUrl` automatically normalizes `/v1beta` path prefix.
- Media permission revalidation & ETag: `GET /api/media/:id` returns `Cache-Control: private, no-cache` and an ETag for both 200 and 304. A matching ETag must still require current-user access; switching to an unauthorized account must return 403. Force-refresh old pages once to replace previously cached immutable responses.
- Media batch deletion: `removeUnreferencedMedia` performs batch status claims, batch reference checks, and single-batch MinIO `removeObjects` calls, replacing serial per-image round-trips.
- Channel scheduler lock contention: `markChannelResult` only updates `channels` rows when recovering from failure/cooldown, eliminating hot-row lock contention across concurrent workers.
- Auth dual-dimension rate limit: Login enforces pure IP rate limiting (30 failed per 5m) alongside per-(IP+username) limit; `SimpleRateLimiter` enforces a hard cap of 5000 with LRU eviction.
- Security headers & batch task queue limits: Standard security headers (`X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`) are injected on all API responses; `POST /api/generation-batches` blocks submission if the user already has 50 active tasks.
- Text route retry & message error fix: Failed text requests can be retried with the same `requestId` without crashing with 409 conflict; attachment size error message reflects dynamic `MAX_UPLOAD_BYTES`.
- Route code splitting: `/admin/*` (6 pages), `/user/*` (4 pages), `/canvas/:id`, and `/image` now load as separate chunks so regular users no longer download the admin console up front; on first visit the sidebar and header appear immediately with a centered spinner only in the content area, and behavior after load is unchanged; unauthenticated redirects, `AdminGuard` enforcement, and the `/user` / `/admin` default redirects must still work as before.
- Parallel admin usage statistics: the 7 analysis queries behind `GET /api/admin/stats` (queue, storage, text totals, overall totals, per user/model/channel) now run concurrently; the response shape and values must be identical to before, with noticeably lower latency over large date ranges.
- New performance indexes (`0008_perf_indexes`): `media_objects_orphan_idx` is a partial index for orphan media cleanup (`status='ready' and reference_count=0`) so the daily sweep no longer scans the whole table; `assets_updated_pagination_idx` covers the asset list's `updated_at desc, id desc` ordering for faster pagination. After migrating, asset list ordering and orphan cleanup behavior must remain unchanged.

- Canvas save reliability: new `0007_canvas_project_history` migration means canvas saves no longer fail on a fresh deployment due to the missing table; Fastify `bodyLimit` now matches `MAX_UPLOAD_BYTES`, so large canvases (snapshots over 1MB) save successfully and oversized payloads return 413 instead of 500; failed saves now retry at most 5 times with exponential backoff (2s up to 30s), stop retrying immediately on 4xx (413/400), surface a red indicator in the canvas top bar with manual retry, and never discard the pending edit.
- Canvas project list & on-demand snapshots: `GET /api/canvas-projects` returns only title, timestamps, and node/connection counts while project cards still show correct stats; the full snapshot is fetched only when a canvas is opened, and saves are blocked until the snapshot has loaded (so an unloaded canvas can never overwrite server data with an empty one); restoring a history revision now only syncs local state instead of writing back again.
- Canvas auto-backup throttling: auto-backup writes a snapshot only when content actually changed (ignoring viewport) and at least 5 minutes have passed since the last auto-backup; panning or zooming alone no longer creates history entries; manual snapshots and restores still always back up the current revision, and the 20-revision cap is unchanged.
- User center account actions: updating the display name succeeds and syncs the top navigation; changing the password succeeds, requires at least 10 characters (matching the server), then logs out and redirects to login.
- Generation history pagination & polling: `/user/generations` uses server-side pagination (50 per page) instead of fetching the entire history; polling runs every 3s only while the current page has queued/running tasks and pauses automatically when the tab is hidden; "save whole batch" reports the actual success count and leaves failures unsaved; "download all" produces a valid archive with correct file names.
- Asset list pagination: `GET /api/assets` accepts `limit`/`offset` with a stable `updatedAt` + `id` sort, and the client pages up to 1000 entries so a large public asset library is never returned in one response.
- Generated image download hardening: image URLs returned by upstream that resolve to private, loopback, link-local (including `169.254.169.254`), CGNAT, or multicast addresses are rejected as failures; redirects are validated hop by hop for up to 4 hops; set `ALLOW_PRIVATE_IMAGE_HOSTS=true` when the relay gateway itself is on an internal network.
- Synchronous text generation timeout: Nginx read/send timeouts for `/api/` are raised to 600s so long text generations are no longer cut off as 504 after 60 seconds.
- Message ordering: multiple messages written in one transaction (identical timestamps) keep a stable, correct order in conversation detail and in the context window.
- Reference image reuse & resource cleanup: tasks in the same batch load reference images from MinIO once and share them, cutting memory use and object storage requests for multi-image batches; expired sessions are physically deleted on a daily timer so the `sessions` table stops growing; request-log retention cleanup returns only the affected row count instead of reading 30 days of primary keys into memory.

- Channel circuit breaker & progressive image rendering: when upstream channels encounter network disconnection, timeout, 5xx, or 429 errors, the scheduler automatically triggers a 2m cooldown and routes subsequent requests directly to healthy channels; canvas and image workbench support progressive rendering as each image finishes without waiting for the full batch.

- Canvas version history & manual snapshot management: top-left canvas menu provides "Version History" entry allowing users to create annotated snapshots, browse historical revisions (distinguishing auto-backups from manual snapshots with node/connection counts and timestamps), and perform one-click rollbacks; frontend enforces hydration write locks to prevent empty overrides and server automatically maintains up to 20 historical revisions.
- Media upload & generation byte size limit increase: Nginx, Fastify, and environment configuration increase default `MAX_UPLOAD_BYTES` and `MAX_GENERATED_BYTES` from 20MB to 50MB, allowing imports of high-resolution image assets over 20MB while returning structured errors upon limit violation.
- NewAPI multi-image generation & canvas visual ordering fix: `/images/edits` upstream requests use standard repeated `image` form fields for compatibility with NewAPI/OneAPI gateways; canvas resource node collection stably sorts connected nodes by visual coordinates ($y$-axis ascending, then $x$-axis ascending); multi-image reference prompt prefix includes aliases (`图1`, `Image 1`) to eliminate model reference ambiguities.
- Image Worker true concurrent consumption: refactored single worker loop with batchSize into N independent worker instances (matching configured concurrency), eliminating queue pull stalls when individual upstream requests take minutes.
- Auth rate limiter memory cleanup: login & change-password sliding window limiter periodically sweeps expired records every 10m and evicts keys on capacity limits, eliminating memory leaks.
- Media stream abort & memory management: image download reader and MinIO stream consumption explicitly invoke `reader.cancel()` / `stream.destroy()` upon byte-limit exceeded or failure to release sockets and buffers immediately.
- Canvas save debounce & flush on unload: canvas auto-save debounce extended to 1000ms to reduce advisory lock contention; `flushProject` triggers on route unmount / window unload to guarantee zero data loss.
- Enterprise architecture stability & concurrency fix: channel slot concurrency uses an in-memory semaphore, freeing DB physical connections during upstream requests (up to 8m) with zero pool exhaustion; pg-boss tasks timeout extended to 3600s with standard policy.
- Auth security & rate limiting: login and change-password endpoints enforce a 5 req/min rate limit per IP and equalize response timings with dummy argon2 verification to eliminate username enumeration side channels; admin prefix routes strictly enforce role checks.
- Text request idempotency & crash recovery: text completions with existing active/completed `requestId` return deduplicated responses; server restarts cleanly recover interrupted text requests and update corresponding `request_logs` to failed.
- Media cleanup and ownership integrity: media garbage cleanup follows safe DB-row delete then MinIO file removal; cross-user asset references are strictly blocked.
- Frontend polling and asset saving: canvas image recovery avoids double-polling active frontend generations; polling sleep responds to AbortSignal; saving assets awaits completion with error handling.
- Channel configuration user feedback: canvas and workbench prompt to contact an administrator when no models/channels are available.

- Internal platform accounts and permissions: bootstrap an administrator, create users, require a password change on first login, disable users, and reset passwords. Disabling or resetting must revoke existing sessions. Regular users must not enter `/admin` or read channel names, base URLs, API keys, priorities, weights, or image prices from model pickers or APIs.
- Internal platform cross-device data: the same account on two devices using the public HTTPS domain should see the same canvas projects, nodes, connections, viewport, assistant messages, private/public assets, image batches, and text results. Canvas edits should save serially after the debounce and recover from refreshes or temporary failures without an older response overwriting a newer snapshot; simultaneous editing intentionally remains last-write-wins.
- Internal platform asset permissions: every user may publish public assets; creators may edit or delete their own entries, administrators may manage all public assets, and other users may only use them. Removing a public entry must not break images already referenced by another user's canvas or private asset, and private data must not be readable across users.
- Internal platform file storage: canvas uploads, drag-and-drop, assets, and text attachments should accept only PNG, JPEG, and WebP. User images over 20 MB and generated results over 50 MB or with unsupported formats must fail; successful uploads and generations should record dimensions and reload from protected MinIO media URLs rather than using browser base64 as authoritative storage.
- Internal platform image jobs: one multi-image action should create one batch containing independent queued tasks with recoverable queued, running, succeeded, and failed states. Jobs must continue after closing or refreshing the page; the canvas Stop action should explicitly stop only local waiting while the server continues processing and billing, then restore results automatically. Retrying a failed canvas slot should requeue its original task without creating another batch; completed non-running batches must be deletable, all history pages must load, and referenced media must remain in MinIO.
- Internal platform image-job recovery: after refresh or stopping local waiting, image children with persisted server batch markers and their configuration parent must retain accurate queued, running, succeeded, and failed states.
- Internal platform routing: strict primary/backup priorities, weighted routing within the same priority, controlled failover for network errors, timeouts, 429, 5xx, 401, and 403, no failover for user input or moderation errors, and at most one extra route for ambiguous errors. Both the global image concurrency limit and each channel concurrency limit should apply.
- Internal platform text flow: canvas text generation and image questions should use only published administrator-managed text models. Nodes should persist and reuse a server `conversationId` and use `textRequestId` to associate the current request exactly; messages, model, actual channel, state, and duration should persist with canvas ownership. Current PNG/JPEG/WebP attachments should reach OpenAI or Gemini without being attached again to history. Reusing the same request ID must not create duplicate conversations, messages, or attachment references; concurrent multi-text generation should settle each child independently, refresh or stopping local waiting should recover the current request and its parent configuration state, and API restarts should mark interrupted text requests failed instead of leaving them running forever.
- Internal platform request logs: the admin console gains a "Request logs" page; every upstream attempt for image jobs, text requests, and channel probes (test/fetch model list), including failover retries, should create one log entry showing time and status, user, type, platform model/channel/upstream model, HTTP status, cost, duration, and error details. Filtering by time range, user, model, channel, type, and status with server-side pagination should work; clicking a detail cell should copy the full request information; the list should auto-refresh while entries are running; logs older than 30 days should be cleaned up automatically, and "Clear logs" must only remove history after confirmation.
- Internal platform administration and analytics: administrators should manage public models, channels, upstream mappings, priority, weight, timeout, channel concurrency, and per-image RMB prices, and fetch compatible model lists. Analytics should provide today, 7-day, 30-day, and custom ranges; filters by user, model, and actual channel; image counts, costs, duration percentiles, both success rates, text-call counts, and the live image queue. Filtering by actual channel must still count that channel's failed and failover attempts. Channel rows should show the latest attempt status, duration, upstream model, and error summary. Public assets should support filtering, search, editing, and deletion.
- Internal platform usage analytics fix: after another user successfully generates images, analytics should show the corresponding requests, successful images, cost, and user/model/channel breakdowns. Re-entering or focusing the page should fetch current data, and its horizontal padding should match the other administration pages.
- Internal platform usage analytics layout: on desktop, the 10 metric cards should use five cards per row across two compact rows with noticeably reduced card height; narrow screens should remain readable and responsive.
- Internal platform administration layout: on desktop widths, the model and channel management content should use the available main-column width without excessive outer whitespace; tables, actions, drawers, and dialogs should remain usable at narrow widths.
- Internal platform model setup: the channel table should keep base URLs compact, with narrower channel/base-URL columns and a wider latest-attempt column; latest attempts should fully show error text and start time without truncation, and clicking the cell should copy the complete detail (status, duration, upstream model, error, time). Administrators should be able to fetch and search upstream models, then create a public model or bind an existing one with capability, price, publication status, priority, weight, and enabled state in one flow.
- Internal platform image-model ordering: an administrator-saved model sort value should persist, lower values should appear first, and equal values should use a stable display-name fallback in both the image workbench and canvas model pickers.
- Internal platform model variants: two published models with different display names and prices should be saveable even when both bind to the same upstream identifier (for example, `gpt-image-2`); the user model picker must keep both variants selectable independently.
- Internal platform model deletion: deleting a model should hide it from admin and user model lists, remove its channel bindings, and preserve historical generation/text/statistics records; the deleted model must not become selectable again.
- Gemini image channels: after configuring a Gemini `generateContent` relay such as `https://torchai.ai/v1beta`, generation requests should include the API key as `?key=`, use `generationConfig.imageConfig` with `responseModalities`, and return an image for the relay's Nano Banana model instead of being rejected because of the unsupported `responseFormat` field.
- Gemini safety responses: HTTP 451 or an upstream unsafe-prompt response should be shown as a content-moderation rejection with a clear instruction to revise the prompt or reference image, without failover.
- Model picker icons: platform model variants selected by UUID should render provider icons from their display names, including OpenAI for GPT Image and Gemini for Banana/Nano Banana/Imagen models, with CPU retained only as the unknown-model fallback.
- Local Agent restoration: authenticated user pages should expose the resizable Agent panel from the top navigation, canvas pages should show the Agent button and compact connection state, and an existing local token should reconnect silently. Agent URL, token, Codex model, skills, history, logs, and canvas operations must remain browser/local-Agent concerns and must not be stored in platform channel configuration; administration pages should not mount the Agent panel.
- Platform deployment and recovery: Docker Compose starts Web, API, PostgreSQL, and MinIO behind the 1Panel public HTTPS domain; source ports 3000/3001, PostgreSQL, and MinIO are not publicly accessible. Login, Secure cookies, reverse proxying, and health checks require deployment verification. PostgreSQL and MinIO must be backed up and restored as one recovery point, with a separate channel-encryption key backup.
- Docker API queue recovery: when an existing `generate-image` queue does not use the `short` policy, restarting the API should update the policy in place while preserving its partition and jobs; the container should pass its health check instead of repeatedly restarting on a foreign-key error.
- Internal platform image-editing focus: normal reference-image editing should continue to work. Mask editing currently sends the mask as an additional upstream reference, so each target channel needs explicit manual verification for expected mask semantics, output quality, and error handling.
- Channel scheduler performance: when channel slots are saturated, requests should queue for a slot (up to about half the channel timeout) and then fail over; per-channel concurrency limits must still hold exactly. Creating generation/text requests with all channels cooling down should still report "no available channel", and that precheck must no longer decrypt channel keys.
- Text history truncation and listing: after a conversation exceeds 50 messages, new requests carry only the most recent 50 history entries upstream; the conversation list API returns newest-updated first and supports `limit`/`offset` with a default of 100.
- Upstream timeout and size message: slow or stalled upstream response bodies should fail after the channel timeout and allow failover instead of waiting forever; after changing `MAX_GENERATED_BYTES` to a non-50MB value, oversized-image errors should show the configured size.
- Frontend batch polling and model cache: image batches with no progress stop polling after 10 minutes with a hint to check generation history later, while poll intervals relax from 700ms to 3s; during repeated generation/text requests `/api/models` should be requested at most once within 60 seconds.
- Statistics and queue stability: per-user/per-model statistics stay fast on larger datasets; restarting the API to recover queued tasks must not enqueue duplicate no-op jobs (verifiable via pgboss job counts across restarts); enqueueing a 20-image batch should be noticeably faster than serial dispatch.
- Workbench multi-batch parallelism: while one batch runs, submitting a new batch and previewing other records should both work without interruption; the log list shows live progress for every running batch, and first page load issues a single list request carrying per-batch success/failure/thumbnail summaries.
- Completion notifications and bulk actions: finishing a batch in a background tab should trigger a system notification and set the tab title to "✓ Images generated", restoring on focus; with multiple failures a "Retry failed (n)" button appears; with multiple successes a "Download all" button produces a zip numbered sequentially.
- Download naming and guidance: single downloads use filenames with prompt prefix, timestamp, and index; an empty model list shows guidance to contact an administrator; creating requests while all channels cool down includes an administrator hint in the error.
- Channel testing and credential handoff: the new "Test" button reports the upstream model count without opening the config drawer; channels cooled down by 401/403 show a cooldown tag; creating users or resetting passwords opens a one-time credential card (username + temporary password + copy) that cannot be viewed again after closing.
- Roles and announcements: admins can toggle user roles in either direction (not on themselves); role/status filters work; a Platform Admin header button publishes announcements shown as a dismissible banner atop every user workspace page; stats tables support header sorting and CSV export, auto-refreshing every 5 seconds while tasks are queued.
- Announcement and changelog system: opening or acknowledging announcements marks them seen and prevents repeated popup alerts across refresh; tabs auto-select changelog when notice text is empty; admin editor supports changelog reordering (move up/down), quick today date insert, tag selector, clear all confirmation, full preview, and force re-alerting all users; MarkdownLite correctly renders fenced code blocks, safe images, and strikethrough text in both light and dark themes.
- User Center & Personal Management: authenticated users can access the new User Center from the account menu at the top-right corner; the dashboard includes Generation History with keyword search and status filters (all, temporary, permanent, expiring soon), expiration countdown tags, one-click 'Remix in Workbench' with auto-populated prompt/model, batch and individual 'Save to Assets', zip batch download, prompt copying, and batch deletion; Usage Analytics card with storage health breakdown and success rates; personal Request Logs with type/status filters and error copying; Profile & Security page supporting display name editing and password updates; and AnnouncementCenter integration in the User Center header.
- Storage usage and orphan cleanup: the account menu should show "my usage: N images · size" matching the "Object storage" metric card on admin analytics; an uploaded reference never submitted anywhere should be cleaned automatically once older than the grace period (7 days by default) without cleanup errors in server logs; files referenced by canvases, assets, batches, or messages must not be removed.
- Local proxy: Settings should show a dedicated Local proxy tab between Providers and Preferences, with a switch that is off by default. Turning it on should reveal the `npx @basketikun/canvas-proxy@latest` command (with `--port` appended when a non-default port is configured), a copy action, and an address field defaulting to `http://127.0.0.1:23210` that normalizes a missing scheme and trailing slashes on blur. Test connection should succeed only while the command is running. With the proxy on, fetching model lists, generating images, videos, text (including streamed output), and audio, running custom call scripts, polling video tasks, downloading generated images and videos, and WebDAV connection tests plus sync should all route through the proxy and keep working; turning it off should restore direct requests. The proxy itself should forward request methods, headers, query strings, and bodies unchanged, stream SSE responses without buffering, and keep `data:` and `blob:` URLs untouched.
- Image and video size settings: Image settings should split resolution (`1k` / `2k` / `4k` / auto) from aspect ratio (`1:1`, `2:3`, `3:2`, `4:3`, `3:4`, `16:9`, `9:16`, `21:9`, `9:21`, auto) and fill W/H from that pair. Video settings should keep `480p` / `720p` / `1080p` (plus a custom `p` value) and `1:1` / `3:4` / `4:3` / `16:9` / `9:16` / `21:9` / auto, then fill W/H from that pair. Duration should be a 4–30 second slider with the current value on the right; auto ratio should disable W/H.
- Built-in video requests: without a custom call script, OpenAI-format channels should send `mode`, `first_frame`/`last_frame` or `image[]`, `generate_audio`, `watermark`, and reference videos/audio like the insertable template, and must not use `input_reference[]`. Gemini-format channels should use `predictLongRunning` with `image`/`lastFrame` or `referenceImages`, and must not report that video is unsupported. Refreshing the page should continue polling a Gemini task.
- Video generation mode: Video settings should offer First & last frame and General reference. One image is sent as the first frame, two images as first and last frames, and more than two images as general references even if first-and-last-frame mode is selected. Built-in requests and request scripts should use `frames` or `reference`.
- Gemini image generation: Gemini-format channels and the Gemini image call-script template should send `generationConfig.responseModalities: ["TEXT", "IMAGE"]` with `imageConfig.aspectRatio` / `imageConfig.imageSize`, and must not send `responseFormat`.
- Video request scripts: opening Call Script for a Video model should open a full-screen stepped editor. The left panel should follow Read the rules → Let another AI write it → Available variables, and list `videos` (File[]) and `audios` (File[]). Copy script brief should include the capability, variables, return requirements, and writing rules. Inserted templates should be async functions whose parameter lists unpack `params` (image templates must include `quality`, `size`, `count`, and `background`). The OpenAI video template should use FormData with `mode`, duration, size, resolution, audio, watermark, and reference images/videos/audios; `mode=frames` should send `first_frame` / `last_frame`, and `mode=reference` should send `image[]`, then poll and download `/content`. After pasting a script from another AI and saving, generating from the canvas with connected reference videos or audio should pass those files into the script.
- URL credential import: opening the app with a new valid `baseUrl` and `apiKey` should append a provider without changing existing providers; importing the same Base URL again, including trailing-slash or `/v1` variants, should update only that provider and remain idempotent; an API key without a Base URL or an invalid Base URL should leave all settings unchanged.
- Documentation navigation and page contents: at phone width, opening the documentation sidebar should show the current section and its description at the top. Expanding it should allow switching among Overview, Canvas Guide, Development, Progress, Business, and Support; the selected section and page list should update together while the desktop top tabs remain unchanged. At widths of 960px and above, a table of contents with hierarchical track lines should appear to the right of the article, support heading navigation, and highlight the current heading while scrolling; narrower layouts should keep the top collapsible contents control.
- Canvas Agent generation-flow references: when a generation prompt contains only `@[node:...]` references already passed through `referenceNodeIds`, the existing text, image, video, audio, or group nodes should connect directly to the generated config node without creating a duplicate prompt text node; prompts containing ordinary prose should continue creating their own prompt text node.
- Video frame capture: right-clicking a video node with content should show Capture first frame, Capture last frame, and Capture current frame. Each action should create and select a proportional image node to the right, connect the video to the image, use the player's visible position for the current frame, and leave the original playback position unchanged.
- Text nodes: the Generate button should no longer appear in the upper-right corner of the node body, and text should use the full content area. The image-generation action in the hover toolbar should continue to create a generation configuration.
- Multi-text generation: requesting more than one result should create one text-result node and one source connection. Its upper-right count should expand all successful alternatives like a multi-image node; setting an alternative as primary should immediately replace the body while retaining the previous primary, and edits to any primary text should remain when switching away and back.
- Group references: group nodes should use a transparent, lightweight border and title treatment and expose a source handle on the right. Connecting a group to a generation node should include every populated image, video, audio, text, and reference-capable extension node inside it. Regular prompt mentions should list the group's resources, while the generation-config composer should allow selecting the whole group with one `@`; resources referenced both individually and through the group must be sent only once.
- Group selected nodes: after selecting two or more nodes, a rounded dashed bound should wrap the whole selection, with Group above it. It wraps those nodes in a group without moving them, then selects the new group. Selecting a group should allow Ungroup from the hover toolbar, context menu, or `Ctrl/Cmd+Shift+G`. Nodes already in the same group should show Ungroup instead of grouping again. Dragging into a group, group references, and the left-panel tree should keep working.
- Node reference bar: regular prompt panels, expanded editors, and generation-config inputs should show their current input nodes above the editor. Images and videos should use thumbnails with hover previews, while text, audio, and extension nodes show their matching type. A group connection should expand into every valid content node inside the group, and removing any expanded item should disconnect the whole group. The plus button should enter canvas selection for adding multiple valid references. Connected nodes, the target itself, children already referenced through a group, and unsupported nodes must not be selectable again; ESC or the top hint should return to the input, and new connections should immediately update the `@` list.

- Multi-image transparency: when a multi-image node containing transparent PNG or WebP assets is expanded, transparent areas in both the primary and alternatives should reveal the canvas; switching primary images must not introduce a white or panel-colored background.
- Canvas navigation: the canvas should open in Move mode by default, and the bottom toolbar should switch between Select and Move modes and reflect the active mode with its icon; Select should draw a clearly spaced dashed range whose dash length, gap, and stroke width remain visually constant at every zoom level, while Move should drag elements directly and pan only when dragging the blank background. Clicking blank canvas space in either mode should deselect the current node and close its top toolbar and lower prompt panel. Holding Control or Space should temporarily invert the active tool without changing the toolbar mode: Select becomes Move, and Move becomes range selection. After clicking any canvas-page button, pressing Space should only invert the tool and must not trigger that button again. Middle-button panning from any position and additive node selection with Shift/Cmd should continue to work, while Space remains available inside text inputs and editable content.
- English routes without a locale prefix should load directly without a redirect loop; switching between English and Simplified Chinese should keep navigation, settings, titles, descriptions, Ant Design components, and persisted preferences in the selected language.
- Global Dropdown, Menu, Select, Cascader, and TreeSelect popup backgrounds, hover states, and selected states in both light and dark themes.
- Canvas node resize stability: importing an image should open its prompt panel, resizing it immediately from any corner should temporarily hide the panel, remain smooth without flashing or triggering a maximum update depth error, and restore the panel after release. Prompt scrolling, generated-prompt restoration, edit-prompt persistence after closing an existing image or text node panel, expanded prompt editing with live synchronization and `@` references, image editing, drag-and-drop references, and generation configuration should continue to work.
- Text output count: both a text node's lower prompt panel and a generation configuration node in Text mode should expose an independent generation count from 1 to 15, default to one, and create that many text nodes. Changing the text count must not alter the image count, and switching a generation configuration node back to Image should continue to use the configured image count.
- Node editing entry points: the image toolbar should default to icons without labels and no longer include Edit; the text toolbar should no longer include Edit or Edit text. Clicking an image, text, or video node without dragging should open its lower prompt panel directly, while image toolbar labels can still be enabled explicitly from the toolbar settings.
- Canvas side panel: clicking an element row should smoothly center and select its node without zooming above 100%; image elements with content should still open the large preview without triggering canvas focus.
- 画布左侧树形列表：组内节点应缩进显示在所属组下方，并带有清晰的层级连接线；组箭头可以收起和展开子节点，搜索或类型筛选时仍应保留匹配节点所属的组。
- Multi-image canvas generation: starting N images should immediately create one image node with N expandable slots and one incoming connection; each empty slot should independently show generating or failure while requests are running, every slot with image content should always display that image, and the first successful image should appear as the primary image immediately. The collapsed state should use a compact stack with at most three closely spaced backplates and a clearly legible count control; only clicking the count control should expand or collapse the slots upward and to the right, while double-clicking the image should open its large preview. Every completed secondary image should keep a clearly legible primary-image action visible in both themes. Expanded groups should remain expanded when clicking elsewhere, dragging, resizing, or previewing, and multiple groups may remain expanded together.
- Multi-image task-association recovery: if a persisted image slot cannot find its corresponding server task, it should settle to a failed state instead of spinning forever.
- Multi-image primary selection: choosing another primary image should preserve the node center and current maximum edge while adapting to the new image ratio; free-resize nodes should keep their exact dimensions, and the viewport zoom indicator must not change.
- Failed multi-image slots: every failed slot, including the current primary slot, should expose retry and delete actions. Retry should update only that slot without replacing an existing successful primary image; delete should remove only that failed slot and update the displayed count.
- Multi-image resize: resizing the root node while child images are expanded should keep the group expanded and resize normally without triggering nested React updates or an application error.
- Multi-image alternatives: expanded images should use up to four columns so larger batches stay compact; double-clicking any completed child image should preview that exact image without changing the primary image. Every completed image, including the primary image, should provide an independent download action, while alternatives should also keep clearly legible create-copy and set-primary actions in both themes. Create-copy should add an independent image node to the right of the group, select it, and leave the original image group unchanged.
- Canvas Agent startup, MCP status, model and permission controls, approvals, version reporting, diagnostics, streaming responses, history consistency, multi-tab isolation, and local Skill management.
- Canvas Agent credential redaction: Codex and Claude CLI stderr containing Bearer tokens, `sk-` keys, explicit API key/token/authorization assignments, ANSI formatting, or credentials split across output chunks should reach neither debug logs nor browser diagnostic events in plaintext; ordinary stderr text should remain intact.
- Image API model parameters: `gpt-image-*` generation and edit requests should omit `response_format`, while `dall-e-*` requests should continue sending `response_format: "b64_json"`.
- Image URL persistence: canvas and image-workbench generation should download each remote API result only once, validate it as a decodable image, and persist it in IndexedDB; `200` responses containing non-image data should fail, stopping canvas generation during download should not save or apply the result, CORS-blocked but displayable images should keep their original URL without an empty storage key, and plugin calls should still receive Data URLs whenever the remote image is readable.
- Multi-reference image editing: OpenAI-format edits with more than one reference image should submit files as repeated `image[]` form fields; a single reference should continue using `image`. Newly created OpenAI image scripts should use the same field names. Gemini-format channels and already saved custom scripts are unchanged.
- Agent header tabs in English and Chinese at different panel widths; labels should collapse to icons and counts before either edge is clipped.
- Agent message layout, Markdown, code blocks, attachments, token statistics, progress timelines, command groups, and scroll-follow behavior.
- Agent inline references: type `/` to select an enabled Skill or `@` to select a canvas asset at the caret; tokens should remain aligned with text, support keyboard navigation and hover previews, and preserve their positions after sending or refreshing.
- Agent message metadata: image attachments, Skills, canvas references, and local previews should survive page refreshes and Agent restarts through exact `clientMessageId`, `threadId`, and `turnId` ownership; protocol upgrades must not reset the independent storage format, unknown or damaged storage must refuse writes without overwriting files, no records may be silently pruned by count or size, and deleting a thread should remove its metadata and preview assets without relying on browser IndexedDB or text matching.
- Prompt Center layout, source synchronization, search, asset insertion, prompt detail dialogs, and compact split-layout prompt pickers in the image and video workbenches.
- Workbench history image cleanup: after generating content in the image or video workbench, deleting an asset, canvas image node, canvas content, or canvas project must not remove thumbnails, results, or references from generation history; deleting a generation record should still remove its dedicated local files.
- Prompt text labels: when a regular node or generation configuration is connected to several text nodes, every upstream text block appended to the prompt should start with its label such as `【Text 1】` or `【Text 2】`, numbered in connection order and matching both the `@` references in the prompt and the reference bar. A single text input should still be labeled, and image, video, and audio numbering must stay unchanged.
- Local mask editing: the dialog footer should offer Reset, Export to canvas, and Generate now. Both actions require a painted area and an edit request; Export to canvas should only write the nodes and connections while prefilling the composed prompt in the result node's prompt panel for manual generation, and Generate now should start the request right after writing the nodes. After painting an area and entering an edit request, confirming should add a mask annotation node below the source image showing the original picture with a translucent blue overlay on the painted area that matches the color and opacity used while painting in the dialog, create the result node to the right, and connect both the source image and the mask annotation to it. The request should be sent as two ordinary reference images with a prompt that names them by index, without the `mask` API parameter, and it should also work for Gemini-format channels; only the marked area should change, and the result must not keep any blue overlay.
- Settings import/export and WebDAV-related configuration.
- WebDAV canvas deletion: deleting a previously synced canvas and syncing again should not restore it from WebDAV; canvases created on another device and not deleted should still appear after sync. Refreshing before the next sync should still keep the deletion. Asset and generation-history deletions are unchanged.
- Local storage settings: the Settings page should provide a Local storage tab that reads only the `infinite-canvas` database and shows IndexedDB usage, total site usage, browser quota, quota percentage, and per-object-store record counts and content size; refreshing after local data changes should update the figures without blocking the rest of the page.
- Asset image download: opening an image that still previews in My Assets and clicking Download image should save the file. Titles with Chinese characters, spaces, or brackets such as `【】` should remain the downloaded filename and must not need renaming. Images saved from the canvas in the same session should download as well.
- Canvas video task resume: starting an OpenAI-style video generation should store the remote task ID on the video node. After a page refresh, a still-pending video should keep polling automatically; if polling stopped, **Get task status** should resume with that task ID until the video is ready, the task fails, or the status request errors. Task failure should then allow a normal retry that creates a new generation.

The Chinese version contains the complete item-by-item acceptance checklist.

## 图片分辨率尺寸表

选择分辨率和宽高比后，W/H 按下表填写，单位为像素。预设尺寸不受「16 倍数对齐」开关影响；该开关只控制手动输入尺寸时的对齐。最终生成尺寸以所选模型的实际输出为准。

| 宽高比 | 1K | 2K | 4K |
| --- | ---: | ---: | ---: |
| **1:1** | **1024 × 1024** | **2048 × 2048** | **2880 × 2880** |
| **2:3** | 1024 × 1536 | 1360 × 2048 | 2336 × 3520 |
| **3:2** | 1536 × 1024 | 2048 × 1360 | 3520 × 2336 |
| **4:3** | 1024 × 768 | 2048 × 1536 | 3312 × 2480 |
| **3:4** | 768 × 1024 | 1536 × 2048 | 2480 × 3312 |
| **16:9** | 1536 × 864 | 2048 × 1152 | **3840 × 2160** |
| **9:16** | 864 × 1536 | 1152 × 2048 | **2160 × 3840** |
| **21:9** | 2016 × 864 | 2688 × 1152 | **3840 × 1648** |
| **9:21** | 864 × 2016 | 1152 × 2688 | **1648 × 3840** |

待验收：

- 在画布和图像工作台逐项切换 3 档分辨率与 9 种比例，W/H 应与表格一致，重新打开设置后仍选中正确档位；重点检查 1K 的 2:3、16:9、21:9 及对应竖图，以及 4K 的 1:1。
- 所有 4K 预设提交时均不应触发本地总像素或最长边限制；生成和编辑请求、调用脚本应收到所选像素尺寸。
- 分辨率为自动时保留比例选择，比例为自动时禁用 W/H；手动输入 W/H 后，对齐开关仍应按原有方式工作。
- Gemini 在质量为自动时，应将表中尺寸识别为对应的 1K / 2K / 4K；显式选择质量时维持原有优先级。视频尺寸设置应保持原有行为。
